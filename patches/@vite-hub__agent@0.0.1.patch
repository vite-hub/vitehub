diff --git a/dist/harness/codex.js b/dist/harness/codex.js
index cd6382032df1a5d5d1a054a129499f98e41c0d89..430f971f3049946431c1e7cf55f10632f1d71d34 100644
--- a/dist/harness/codex.js
+++ b/dist/harness/codex.js
@@ -8,6 +8,7 @@ import { homedir, tmpdir } from "node:os";
 import { createCodex } from "@ai-sdk/harness-codex";
 //#region src/harness/codex.ts
 const harnessSandboxAdapter = Symbol.for("vitehub.harnessSandboxAdapter");
+const harnessSkillsDirectory = Symbol.for("vitehub.harnessSkillsDirectory");
 function codexDriver(options = {}) {
 	const { authJson, authJsonPath, credentials, env, instructions, sandbox, workDir, ...settings } = options;
 	const defaultOpenAIAuth = settings.auth === void 0;
@@ -53,6 +54,7 @@ const codexBridgeLockReplacements = {
 function createViteHubCodex(settings, preferOpenAI) {
 	return {
 		...createCodex(settings),
+		[harnessSkillsDirectory]: "tmp/harness/codex-home",
 		[harnessSandboxAdapter]: (provider) => relativeCodexSandboxProvider(provider, { preferOpenAI }),
 		async getBootstrap() {
 			const [pkg, lock, bridge, hostToolMcp] = await Promise.all([
@@ -111,12 +113,17 @@ function patchCodexBridge(bridge) {
 }
 function relativeCodexSandboxSession(session, bootstrapDir) {
 	const anchoredBootstrapDir = bootstrapDir ?? `${session.defaultWorkingDirectory.replace(/\/+$/, "")}/${localCodexBootstrapDir}`;
+	const codexHome = `${session.defaultWorkingDirectory.replace(/\/+$/, "")}/tmp/harness/codex-home`;
 	const rewriteCodexBootstrapPath = (value) => value.replaceAll(codexBootstrapDir, anchoredBootstrapDir);
 	return new Proxy(session, { get(target, property, receiver) {
 		if (property === "restricted") return () => relativeCodexSandboxSession(target.restricted(), anchoredBootstrapDir);
 		if (property === "run" || property === "spawn") return (options) => target[property]({
 			...options,
-			command: rewriteCodexBootstrapPath(options.command)
+			command: rewriteCodexBootstrapPath(options.command),
+			env: {
+				...options.env,
+				CODEX_HOME: codexHome
+			}
 		});
 		if (property === "readBinaryFile" || property === "readFile" || property === "readTextFile" || property === "writeBinaryFile" || property === "writeFile" || property === "writeTextFile") {
 			const method = target[property];
@@ -130,6 +137,10 @@ function relativeCodexSandboxSession(session, bootstrapDir) {
 		return typeof value === "function" ? value.bind(target) : value;
 	} });
 }
+async function prepareCodexHome(session) {
+	const result = await session.run({ command: 'mkdir -p "$CODEX_HOME" && chmod 700 "$CODEX_HOME" && : > "$CODEX_HOME/config.toml" && if [ -f "$HOME/.codex/auth.json" ]; then ln -sfn "$HOME/.codex/auth.json" "$CODEX_HOME/auth.json"; fi' });
+	if (result.exitCode !== 0) throw new Error(`[vitehub] Failed to prepare isolated Codex home: ${result.stderr || "sandbox command failed"}`);
+}
 function codexSandboxProvider(options) {
 	const { sandbox } = options;
 	if (sandbox === false) return;
@@ -160,9 +171,11 @@ function relativeCodexSandboxProvider(provider, options = {}) {
 			const onFirstCreate = createOptions?.onFirstCreate;
 			return adaptSession(await provider.createSession({
 				...createOptions,
-				...onFirstCreate ? { onFirstCreate: async (session, context) => {
-					await onFirstCreate(adaptSession(session), context);
-				} } : {}
+				onFirstCreate: async (session, context) => {
+					const adapted = adaptSession(session);
+					await prepareCodexHome(adapted);
+					await onFirstCreate?.(adapted, context);
+				}
 			}));
 		}
 	};
diff --git a/dist/harness-agent-C_ZqPqpj.js b/dist/harness-agent-C_ZqPqpj.js
index 3a749a030208271aa70779b05bba972b29f11f2e..7ead086db523b0cf10df695f81e011ec3d8bd4f6 100644
--- a/dist/harness-agent-C_ZqPqpj.js
+++ b/dist/harness-agent-C_ZqPqpj.js
@@ -11,6 +11,7 @@ const harnessResumeStates = /* @__PURE__ */ new WeakMap();
 const harnessGeneratedFiles = ["harness-tool.mjs"];
 const harnessInstructionFiles = ["AGENTS.md", "CLAUDE.md"];
 const harnessSandboxAdapter = Symbol.for("vitehub.harnessSandboxAdapter");
+const harnessSkillsDirectory = Symbol.for("vitehub.harnessSkillsDirectory");
 function hasEntries(value) {
 	return typeof value === "object" && value !== null && Object.keys(value).length > 0;
 }
@@ -289,7 +290,7 @@ async function createHarnessAgent(options, context, prepareWorkspaceSession) {
 				...workDir !== void 0 ? { workDir } : {},
 				onSession: async ({ abortSignal, session, sessionWorkDir }) => {
 					setActiveHarnessWorkspaceFiles(context.context, activeHarnessWorkspaceFiles(session, sessionWorkDir, abortSignal));
-					await prepareWorkspaceSession(session, sessionWorkDir, abortSignal);
+					await prepareWorkspaceSession(session, sessionWorkDir, abortSignal, harness[harnessSkillsDirectory]);
 				}
 			},
 			permissionMode: defaultHarnessPermissionMode(harness),
@@ -300,6 +301,43 @@ async function createHarnessAgent(options, context, prepareWorkspaceSession) {
 		workDir
 	};
 }
+function agentSkills(context) {
+	return context.skills || {};
+}
+async function installHarnessSkills(context, session, sessionWorkDir, abortSignal) {
+	const sources = agentSkills(context);
+	if (!Object.keys(sources).length || typeof sessionWorkDir !== "string" || !sessionWorkDir) return false;
+	const { prepareHarnessWorkspaceSession, useWorkspace } = await import("@vite-hub/workspace");
+	const workspace = useWorkspace("__vitehub_agent_skills", {
+		definition: {
+			name: "__vitehub_agent_skills",
+			runtime: "trusted-host",
+			sources,
+			store: { provider: "memory" }
+		},
+		mode: "read"
+	});
+	const stagingDirectory = `${sessionWorkDir.replace(/\/+$/, "")}/.vitehub-agent-skills`;
+	const prepared = await prepareHarnessWorkspaceSession(workspace, {
+		abortSignal,
+		paths: ["skills"],
+		session,
+		sessionWorkDir: stagingDirectory
+	});
+	try {
+		const result = await session.run({
+			abortSignal,
+			command: 'mkdir -p skills && cp -R .vitehub-agent-skills/skills/. skills/ && find skills -type f -path "*/scripts/*" -exec chmod +x {} + && rm -rf .vitehub-agent-skills',
+			workingDirectory: sessionWorkDir
+		});
+		if (result.exitCode !== 0) throw new Error(result.stderr || "sandbox command failed");
+	} catch (error) {
+		await prepared.close(error);
+		throw new Error(`[vitehub] Failed to install Agent Skills: ${error instanceof Error ? error.message : String(error)}`, { cause: error });
+	}
+	await prepared.close();
+	return true;
+}
 function hasDetach(session) {
 	return typeof session.detach === "function";
 }
@@ -520,25 +558,30 @@ function createHarnessAgentAdapter(options) {
 	}
 	async function createAgentAndSession(context) {
 		let workspaceSession;
-		const resolved = await createHarnessAgent(options, context, async (session, sessionWorkDir, abortSignal) => {
-			if (!context.workspace) return;
-			const harnessInstructions = await resolveHarnessInstructions(context);
-			const { prepareHarnessWorkspaceSession } = await import("@vite-hub/workspace");
-			const commitDefinition = context.workspaceDefinition && context.workspaceAutoCommit !== void 0 ? workspaceDefinitionWithAutoCommitRules(context.workspaceDefinition, context.workspaceAutoCommit) : context.workspaceDefinition;
-			workspaceSession = await prepareHarnessWorkspaceSession(context.workspace, {
-				abortSignal,
-				...hasWorkspaceCommitRules(commitDefinition) ? { definition: commitDefinition } : {},
-				ignoreWriteBackPaths: harnessWriteBackIgnorePaths(context, harnessInstructions),
-				onWriteBack: (diff) => setHarnessWorkspaceDiff(context.context, diff),
-				paths: selectedWorkspaceScopePaths(context, commitDefinition),
-				session,
-				sessionWorkDir
-			});
+		const resolved = await createHarnessAgent(options, context, async (session, sessionWorkDir, abortSignal, globalSkillsDirectory) => {
+			const harnessInstructions = context.workspace ? await resolveHarnessInstructions(context) : void 0;
 			try {
-				await writeHarnessInstructionFiles(session, sessionWorkDir, abortSignal, harnessInstructions);
-				if (harnessInstructions) await workspaceSession.refreshGitBaseline?.();
+				if (context.workspace) {
+					const { prepareHarnessWorkspaceSession } = await import("@vite-hub/workspace");
+					const commitDefinition = context.workspaceDefinition && context.workspaceAutoCommit !== void 0 ? workspaceDefinitionWithAutoCommitRules(context.workspaceDefinition, context.workspaceAutoCommit) : context.workspaceDefinition;
+					workspaceSession = await prepareHarnessWorkspaceSession(context.workspace, {
+						abortSignal,
+						...hasWorkspaceCommitRules(commitDefinition) ? { definition: commitDefinition } : {},
+						ignoreWriteBackPaths: harnessWriteBackIgnorePaths(context, harnessInstructions),
+						onWriteBack: (diff) => setHarnessWorkspaceDiff(context.context, diff),
+						paths: selectedWorkspaceScopePaths(context, commitDefinition),
+						session,
+						sessionWorkDir
+					});
+				}
+				const installedWorkspaceSkills = await installHarnessSkills(context, session, sessionWorkDir, abortSignal);
+				await installHarnessSkills(context, session, globalSkillsDirectory, abortSignal);
+				if (context.workspace) {
+					await writeHarnessInstructionFiles(session, sessionWorkDir, abortSignal, harnessInstructions);
+					if (harnessInstructions || installedWorkspaceSkills) await workspaceSession.refreshGitBaseline?.();
+				}
 			} catch (error) {
-				await workspaceSession.close(error);
+				await workspaceSession?.close(error);
 				workspaceSession = void 0;
 				throw error;
 			}
diff --git a/dist/src-DTxcX3QT.js b/dist/src-DTxcX3QT.js
index 14ea8cf191d10e529551bfe9c25863c67d454939..f45dca68f8ef1a02cb733d5f56ff8c1d04cfc270 100644
--- a/dist/src-DTxcX3QT.js
+++ b/dist/src-DTxcX3QT.js
@@ -1161,6 +1161,7 @@ async function createAgentInvocationContext(definition, context, input) {
 			finishDeliveryEffectProviders: capabilities.registries.finishDeliveryEffectProviders,
 			finishExtensionProviders: capabilities.registries.finishExtensionProviders,
 			finishHook: definition?.hooks?.["agent:finish"],
+			skills: definition?.[Symbol.for("vitehub.agent.skills")] || {},
 			hasCapabilityCleanup: capabilities.hasCloseCallbacks,
 			handledResponse: capabilities.response,
 			harnessSandboxProvider,
diff --git a/dist/vite.js b/dist/vite.js
index c62621c269dd850c7fc9aac57729eeabe1e2a42e..1fce6488bfc7a11c009110ffcfa4f9d6e348b723 100644
--- a/dist/vite.js
+++ b/dist/vite.js
@@ -2430,7 +2430,7 @@ async function transformScheduleRegistry(code, definitions, agentImportBase, imp
 			`if (Object.prototype.hasOwnProperty.call(registry, ${JSON.stringify(`agent/${definition.name}`)})) throw new Error(${JSON.stringify(`[vitehub] Duplicate Runtime Schedule target: agent/${definition.name}`)})`,
 			`registry[${JSON.stringify(`agent/${definition.name}`)}] = async () => {`,
 			`  const module = await import(${JSON.stringify(handlerImport)})`,
-			`  return vitehubDefineScheduledAgentTarget(vitehubWithWorkspaceSourceRoot(vitehubResolveScheduledAgentModule(module), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}), { agentIdentity: ${JSON.stringify(agentIdentity)}, capabilities: ${generatedAgentRuntimeCapabilities(runtimeCapabilities, true)} })`,
+			`  return vitehubDefineScheduledAgentTarget(vitehubWithWorkspaceSourceRoot(vitehubResolveScheduledAgentModule(module), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))}), { agentIdentity: ${JSON.stringify(agentIdentity)}, capabilities: ${generatedAgentRuntimeCapabilities(runtimeCapabilities, true)} })`,
 			"}"
 		];
 	}))).flat();
@@ -2443,18 +2443,20 @@ async function transformScheduleRegistry(code, definitions, agentImportBase, imp
 		"function vitehubResolveScheduledAgentModule(module) {",
 		"  return module && typeof module === 'object' && 'default' in module ? module.default : module",
 		"}",
-		"function vitehubWithWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions) {",
-		"  const options = agent?.__vitehubWorkspaceAgentOptions",
+		"function vitehubWithWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const skills = Object.fromEntries(Object.entries(colocatedSkills || {}).map(([key, source]) => [key, source.encoding === 'base64' ? { ...source, content: Uint8Array.from(atob(source.content), byte => byte.charCodeAt(0)) } : source]))",
+		"  const resolvedAgent = Object.keys(skills).length ? { ...agent, [Symbol.for('vitehub.agent.skills')]: skills } : agent",
+		"  const options = resolvedAgent?.__vitehubWorkspaceAgentOptions",
 		"  const workspace = options?.workspace",
-		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return agent",
-		"  const existingSources = agent.sources && typeof agent.sources === 'object' ? agent.sources : undefined",
+		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return resolvedAgent",
+		"  const existingSources = resolvedAgent.sources && typeof resolvedAgent.sources === 'object' ? resolvedAgent.sources : undefined",
 		"  const sources = colocatedInstructions",
 		"    ? { __vitehubAgentInstructions: { content: colocatedInstructions, materialize: 'build', mount: '', workspacePath: 'AGENTS.md' }, ...workspace.sources, ...existingSources }",
 		"    : { ...workspace.sources, ...existingSources }",
 		"  const resolvedSources = Object.keys(sources).length ? sources : undefined",
-		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? agent.sourceRootDir ?? sourceRootDir",
+		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? resolvedAgent.sourceRootDir ?? sourceRootDir",
 		"  const workspaceOptions = { ...options, workspace: { ...workspace, ...(resolvedSources ? { sources: resolvedSources } : {}), sourceRootDir: resolvedSourceRootDir } }",
-		"  return { ...agent, ...vitehubWorkspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
+		"  return { ...resolvedAgent, ...vitehubWorkspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
 		"}",
 		...entries,
 		""
@@ -2654,6 +2656,30 @@ async function readColocatedAgentInstructions(handler) {
 		}
 	});
 }
+function readColocatedAgentSkills(handler) {
+	const sourceRootDir = dirname(handler);
+	const skillsRoot = join(sourceRootDir, "skills");
+	if (!existsSync(skillsRoot) || !statSync(skillsRoot).isDirectory()) return;
+	const sources = {};
+	const visit = (directory) => {
+		for (const entry of readdirSync(directory, { withFileTypes: true }).sort((left, right) => left.name.localeCompare(right.name))) {
+			const file = join(directory, entry.name);
+			if (entry.isDirectory()) visit(file);
+			else if (entry.isFile()) {
+				const workspacePath = relative(sourceRootDir, file).replace(/\\/g, "/");
+				sources[`__vitehubAgentSkill:${workspacePath}`] = {
+					content: readFileSync(file).toString("base64"),
+					encoding: "base64",
+					materialize: "build",
+					mount: "",
+					workspacePath
+				};
+			}
+		}
+	};
+	visit(skillsRoot);
+	return Object.keys(sources).length ? sources : void 0;
+}
 function moduleImportSpecifier(fromFile, targetFile) {
 	const specifier = relative(dirname(fromFile), targetFile).replace(/\\/g, "/");
 	return specifier.startsWith(".") ? specifier : `./${specifier}`;
@@ -2767,11 +2793,11 @@ async function generateAgentWebhookRouteHandler(definitions, handlerPath, option
 	const imports = definitions.map((definition, index) => `import * as agent${index} from ${JSON.stringify(moduleImportSpecifier(handlerPath, definition.handler))}`).join("\n");
 	const workspaceEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})` : void 0;
+		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})` : void 0;
 	}))).filter(Boolean).join(",\n  ");
 	const agentEntries = definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})`;
+		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})`;
 		return `${JSON.stringify(definition.name)}: ${agentExpression}`;
 	});
 	const resolvedAgentEntries = (await Promise.all(agentEntries)).join(",\n  ");
@@ -2800,22 +2826,24 @@ async function generateAgentWebhookRouteHandler(definitions, handlerPath, option
 		"  return chatRoute && typeof chatRoute === 'object' ? chatRoute : undefined",
 		"}",
 		"",
-		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions) {",
-		"  const options = agent?.__vitehubWorkspaceAgentOptions",
+		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const skills = Object.fromEntries(Object.entries(colocatedSkills || {}).map(([key, source]) => [key, source.encoding === 'base64' ? { ...source, content: Uint8Array.from(atob(source.content), byte => byte.charCodeAt(0)) } : source]))",
+		"  const resolvedAgent = Object.keys(skills).length ? { ...agent, [Symbol.for('vitehub.agent.skills')]: skills } : agent",
+		"  const options = resolvedAgent?.__vitehubWorkspaceAgentOptions",
 		"  const workspace = options?.workspace",
-		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return agent",
-		"  const existingSources = agent.sources && typeof agent.sources === 'object' ? agent.sources : undefined",
+		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return resolvedAgent",
+		"  const existingSources = resolvedAgent.sources && typeof resolvedAgent.sources === 'object' ? resolvedAgent.sources : undefined",
 		"  const sources = colocatedInstructions",
 		"    ? { __vitehubAgentInstructions: { content: colocatedInstructions, materialize: 'build', mount: '', workspacePath: 'AGENTS.md' }, ...workspace.sources, ...existingSources }",
 		"    : { ...workspace.sources, ...existingSources }",
 		"  const resolvedSources = Object.keys(sources).length ? sources : undefined",
-		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? agent.sourceRootDir ?? sourceRootDir",
+		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? resolvedAgent.sourceRootDir ?? sourceRootDir",
 		"  const workspaceOptions = { ...options, workspace: { ...workspace, ...(resolvedSources ? { sources: resolvedSources } : {}), sourceRootDir: resolvedSourceRootDir } }",
-		"  return { ...agent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
+		"  return { ...resolvedAgent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
 		"}",
 		"",
-		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions) {",
-		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions)",
+		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions, colocatedSkills)",
 		"  if (!workspaceAgentOwnsWorkspaceDefinition(agent)) return",
 		"  return [name, async () => ({ ...module, default: agent })]",
 		"}",
@@ -2869,11 +2897,11 @@ async function generateAgentNetlifyFunctionRouteHandler(definitions, handlerPath
 	const imports = definitions.map((definition, index) => `import * as agent${index} from ${JSON.stringify(moduleImportSpecifier(handlerPath, definition.handler))}`).join("\n");
 	const workspaceEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})` : void 0;
+		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})` : void 0;
 	}))).filter(Boolean).join(",\n  ");
 	const agentEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})`;
+		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})`;
 		return `${JSON.stringify(definition.name)}: ${agentExpression}`;
 	}))).join(",\n  ");
 	const agentModuleEntries = definitions.map((definition, index) => `${JSON.stringify(definition.name)}: agent${index}`).join(",\n  ");
@@ -2910,22 +2938,24 @@ async function generateAgentNetlifyFunctionRouteHandler(definitions, handlerPath
 		"    .replace(/(^|\\/):([^/]+)/g, (_, prefix, key) => `${prefix}${encodeURIComponent(values[key] || '')}`)",
 		"}",
 		"",
-		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions) {",
-		"  const options = agent?.__vitehubWorkspaceAgentOptions",
+		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const skills = Object.fromEntries(Object.entries(colocatedSkills || {}).map(([key, source]) => [key, source.encoding === 'base64' ? { ...source, content: Uint8Array.from(atob(source.content), byte => byte.charCodeAt(0)) } : source]))",
+		"  const resolvedAgent = Object.keys(skills).length ? { ...agent, [Symbol.for('vitehub.agent.skills')]: skills } : agent",
+		"  const options = resolvedAgent?.__vitehubWorkspaceAgentOptions",
 		"  const workspace = options?.workspace",
-		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return agent",
-		"  const existingSources = agent.sources && typeof agent.sources === 'object' ? agent.sources : undefined",
+		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return resolvedAgent",
+		"  const existingSources = resolvedAgent.sources && typeof resolvedAgent.sources === 'object' ? resolvedAgent.sources : undefined",
 		"  const sources = colocatedInstructions",
 		"    ? { __vitehubAgentInstructions: { content: colocatedInstructions, materialize: 'build', mount: '', workspacePath: 'AGENTS.md' }, ...workspace.sources, ...existingSources }",
 		"    : { ...workspace.sources, ...existingSources }",
 		"  const resolvedSources = Object.keys(sources).length ? sources : undefined",
-		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? agent.sourceRootDir ?? sourceRootDir",
+		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? resolvedAgent.sourceRootDir ?? sourceRootDir",
 		"  const workspaceOptions = { ...options, workspace: { ...workspace, ...(resolvedSources ? { sources: resolvedSources } : {}), sourceRootDir: resolvedSourceRootDir } }",
-		"  return { ...agent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
+		"  return { ...resolvedAgent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
 		"}",
 		"",
-		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions) {",
-		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions)",
+		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions, colocatedSkills)",
 		"  if (!workspaceAgentOwnsWorkspaceDefinition(agent)) return",
 		"  return [name, async () => ({ ...module, default: agent })]",
 		"}",
@@ -3000,7 +3030,7 @@ async function generateAgentDiscordGatewayRouteHandler(definitions, handlerPath,
 	const imports = definitions.map((definition, index) => `import * as agent${index} from ${JSON.stringify(moduleImportSpecifier(handlerPath, definition.handler))}`).join("\n");
 	const agentEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})`;
+		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})`;
 		return `${JSON.stringify(definition.name)}: ${agentExpression}`;
 	}))).join(",\n  ");
 	const agentNames = definitions.map((definition) => definition.name);
@@ -3016,18 +3046,20 @@ async function generateAgentDiscordGatewayRouteHandler(definitions, handlerPath,
 		"  return module && typeof module === 'object' && 'default' in module ? module.default : module",
 		"}",
 		"",
-		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions) {",
-		"  const options = agent?.__vitehubWorkspaceAgentOptions",
+		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const skills = Object.fromEntries(Object.entries(colocatedSkills || {}).map(([key, source]) => [key, source.encoding === 'base64' ? { ...source, content: Uint8Array.from(atob(source.content), byte => byte.charCodeAt(0)) } : source]))",
+		"  const resolvedAgent = Object.keys(skills).length ? { ...agent, [Symbol.for('vitehub.agent.skills')]: skills } : agent",
+		"  const options = resolvedAgent?.__vitehubWorkspaceAgentOptions",
 		"  const workspace = options?.workspace",
-		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return agent",
-		"  const existingSources = agent.sources && typeof agent.sources === 'object' ? agent.sources : undefined",
+		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return resolvedAgent",
+		"  const existingSources = resolvedAgent.sources && typeof resolvedAgent.sources === 'object' ? resolvedAgent.sources : undefined",
 		"  const sources = colocatedInstructions",
 		"    ? { __vitehubAgentInstructions: { content: colocatedInstructions, materialize: 'build', mount: '', workspacePath: 'AGENTS.md' }, ...workspace.sources, ...existingSources }",
 		"    : { ...workspace.sources, ...existingSources }",
 		"  const resolvedSources = Object.keys(sources).length ? sources : undefined",
-		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? agent.sourceRootDir ?? sourceRootDir",
+		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? resolvedAgent.sourceRootDir ?? sourceRootDir",
 		"  const workspaceOptions = { ...options, workspace: { ...workspace, ...(resolvedSources ? { sources: resolvedSources } : {}), sourceRootDir: resolvedSourceRootDir } }",
-		"  return { ...agent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
+		"  return { ...resolvedAgent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
 		"}",
 		"",
 		...generatedRuntimeHelpers(),
@@ -3097,11 +3129,11 @@ async function generateAgentDenoServer(definitions, handlerPath, options = {}) {
 	const imports = definitions.map((definition, index) => `import * as agent${index} from ${JSON.stringify(moduleImportSpecifier(handlerPath, definition.handler))}`).join("\n");
 	const workspaceEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})` : void 0;
+		return definition.workspace ? `workspaceRegistryEntry(${JSON.stringify(definition.workspace)}, agent${index}, ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})` : void 0;
 	}))).filter(Boolean).join(",\n  ");
 	const agentEntries = (await Promise.all(definitions.map(async (definition, index) => {
 		const sourceRootDir = resolveWorkspaceSourceRoot(definition.handler);
-		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))})`;
+		const agentExpression = `withWorkspaceSourceRoot(resolveAgentModule(agent${index}), ${JSON.stringify(sourceRootDir)}, ${JSON.stringify(await readColocatedAgentInstructions(definition.handler))}, ${JSON.stringify(readColocatedAgentSkills(definition.handler))})`;
 		return `${JSON.stringify(definition.name)}: ${agentExpression}`;
 	}))).join(",\n  ");
 	const agentModuleEntries = definitions.map((definition, index) => `${JSON.stringify(definition.name)}: agent${index}`).join(",\n  ");
@@ -3134,22 +3166,24 @@ async function generateAgentDenoServer(definitions, handlerPath, options = {}) {
 		"  return chatRoute && typeof chatRoute === 'object' ? chatRoute : undefined",
 		"}",
 		"",
-		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions) {",
-		"  const options = agent?.__vitehubWorkspaceAgentOptions",
+		"function withWorkspaceSourceRoot(agent, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const skills = Object.fromEntries(Object.entries(colocatedSkills || {}).map(([key, source]) => [key, source.encoding === 'base64' ? { ...source, content: Uint8Array.from(atob(source.content), byte => byte.charCodeAt(0)) } : source]))",
+		"  const resolvedAgent = Object.keys(skills).length ? { ...agent, [Symbol.for('vitehub.agent.skills')]: skills } : agent",
+		"  const options = resolvedAgent?.__vitehubWorkspaceAgentOptions",
 		"  const workspace = options?.workspace",
-		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return agent",
-		"  const existingSources = agent.sources && typeof agent.sources === 'object' ? agent.sources : undefined",
+		"  if (!workspace || typeof workspace !== 'object' || 'name' in workspace) return resolvedAgent",
+		"  const existingSources = resolvedAgent.sources && typeof resolvedAgent.sources === 'object' ? resolvedAgent.sources : undefined",
 		"  const sources = colocatedInstructions",
 		"    ? { __vitehubAgentInstructions: { content: colocatedInstructions, materialize: 'build', mount: '', workspacePath: 'AGENTS.md' }, ...workspace.sources, ...existingSources }",
 		"    : { ...workspace.sources, ...existingSources }",
 		"  const resolvedSources = Object.keys(sources).length ? sources : undefined",
-		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? agent.sourceRootDir ?? sourceRootDir",
+		"  const resolvedSourceRootDir = workspace.sourceRootDir ?? resolvedAgent.sourceRootDir ?? sourceRootDir",
 		"  const workspaceOptions = { ...options, workspace: { ...workspace, ...(resolvedSources ? { sources: resolvedSources } : {}), sourceRootDir: resolvedSourceRootDir } }",
-		"  return { ...agent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
+		"  return { ...resolvedAgent, ...workspaceDefinitionFromOptions(workspaceOptions), __vitehubWorkspaceAgentOptions: workspaceOptions }",
 		"}",
 		"",
-		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions) {",
-		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions)",
+		"function workspaceRegistryEntry(name, module, sourceRootDir, colocatedInstructions, colocatedSkills) {",
+		"  const agent = withWorkspaceSourceRoot(resolveAgentModule(module), sourceRootDir, colocatedInstructions, colocatedSkills)",
 		"  if (!workspaceAgentOwnsWorkspaceDefinition(agent)) return",
 		"  return [name, async () => ({ ...module, default: agent })]",
 		"}",
diff --git a/dist/workspace-agent-RiBUhfgs.js b/dist/workspace-agent-RiBUhfgs.js
index f30d2a07606d68f7c77cc579077dc13706465d4b..997791c93eb462cafd9b0c38cbb6fba668b81857 100644
--- a/dist/workspace-agent-RiBUhfgs.js
+++ b/dist/workspace-agent-RiBUhfgs.js
@@ -37,6 +37,7 @@ const defaultWorkspaceName = "workspace";
 const colocatedAgentInstructionsPath = "instructions.md";
 const colocatedAgentInstructionsWorkspacePath = "AGENTS.md";
 const colocatedAgentInstructionsSourceKey = "__vitehubAgentInstructions";
+const agentSkillsSymbol = Symbol.for("vitehub.agent.skills");
 const readCommands = [
 	"pwd",
 	"ls",
@@ -94,12 +95,46 @@ function workspaceAgentOwnsWorkspaceDefinition(agent) {
 	const workspace = (typeof agent === "object" && agent !== null ? agent.__vitehubWorkspaceAgentOptions : void 0)?.workspace;
 	return typeof workspace === "object" && workspace !== null && !isWorkspaceReference(workspace);
 }
+function readColocatedAgentSkills(sourceRootDir) {
+	if (!sourceRootDir) return;
+	const fs = getNodeBuiltin("node:fs");
+	const path = getNodeBuiltin("node:path");
+	if (!fs || !path) return;
+	const skillsRoot = path.join(sourceRootDir, "skills");
+	try {
+		if (!fs.statSync(skillsRoot).isDirectory()) return;
+	} catch {
+		return;
+	}
+	const sources = {};
+	const visit = (directory) => {
+		for (const entry of fs.readdirSync(directory, { withFileTypes: true }).sort((left, right) => left.name.localeCompare(right.name))) {
+			const file = path.join(directory, entry.name);
+			if (entry.isDirectory()) visit(file);
+			else if (entry.isFile()) {
+				const workspacePath = path.relative(sourceRootDir, file).split(path.sep).join("/");
+				sources[`__vitehubAgentSkill:${workspacePath}`] = {
+					content: new Uint8Array(fs.readFileSync(file)),
+					materialize: "build",
+					mount: "",
+					workspacePath
+				};
+			}
+		}
+	};
+	visit(skillsRoot);
+	return Object.keys(sources).length ? sources : void 0;
+}
 function workspaceAgentWithSourceRoot(agent, sourceRootDir) {
-	if (!workspaceAgentOwnsWorkspaceDefinition(agent)) return agent;
-	const workspaceAgent = agent;
+	const skills = readColocatedAgentSkills(sourceRootDir);
+	const workspaceAgent = skills ? {
+		...agent,
+		[agentSkillsSymbol]: skills
+	} : agent;
+	if (!workspaceAgentOwnsWorkspaceDefinition(workspaceAgent)) return workspaceAgent;
 	const options = workspaceAgent.__vitehubWorkspaceAgentOptions;
 	const workspace = options.workspace;
-	if (typeof workspace !== "object" || workspace === null || isWorkspaceReference(workspace)) return agent;
+	if (typeof workspace !== "object" || workspace === null || isWorkspaceReference(workspace)) return workspaceAgent;
 	const resolvedSourceRootDir = workspace.sourceRootDir ?? workspaceAgent.sourceRootDir ?? sourceRootDir;
 	const workspaceOptions = {
 		...options,
@@ -653,14 +688,14 @@ async function resolveWorkspaceMetadataInstructions(options, workspace, resoluti
 	const composed = await composeInstructions(baseInstructions.join("\n\n"), compositionContext, workspaceBindings, coverage);
 	return {
 		instructions: composed ? [composed] : [],
-		warnings: instructionCoverageWarnings(coverage, sourceDefinition, options.capabilities)
+		warnings: instructionCoverageWarnings(coverage, sourceDefinition, options.capabilities, workspaceAgentDriverKind(options))
 	};
 }
-function instructionCoverageWarnings(coverage, definition, capabilities) {
+function instructionCoverageWarnings(coverage, definition, capabilities, driverKind) {
 	return [
 		...sourceCoverageWarnings(coverage, definition),
 		...capabilityCoverageWarnings(coverage, capabilities),
-		...skillCoverageWarnings(coverage, capabilities)
+		...driverKind === "model" ? skillCoverageWarnings(coverage, capabilities) : []
 	];
 }
 function warning(id, primitive, message) {
