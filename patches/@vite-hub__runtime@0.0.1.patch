diff --git a/dist/drain.js b/dist/drain.js
new file mode 100755
index 0000000000000000000000000000000000000000..4af2090272b1321cf72c97aba81120d54d02c3e6
--- /dev/null
+++ b/dist/drain.js
@@ -0,0 +1,50 @@
+#!/usr/bin/env node
+
+const [pidInput, statusUrl = 'http://127.0.0.1:3000/api/drain'] = process.argv.slice(2)
+const pid = Number(pidInput)
+
+if (!Number.isSafeInteger(pid) || pid <= 0) {
+  console.error('usage: node drain.js MAINPID [STATUS_URL]')
+  process.exit(2)
+}
+
+const running = () => {
+  try {
+    process.kill(pid, 0)
+    return true
+  }
+  catch {
+    return false
+  }
+}
+
+const readStatus = async () => {
+  try {
+    const response = await fetch(statusUrl, { signal: AbortSignal.timeout(5_000) })
+    if (!response.ok) return 'unavailable'
+    const body = await response.json()
+    return typeof body?.status === 'string' ? body.status : 'invalid'
+  }
+  catch {
+    return 'unavailable'
+  }
+}
+
+try {
+  let signaled = false
+  while (running()) {
+    const status = await readStatus()
+    if (status === 'failed' || status === 'invalid') throw new Error(`Process drain reported ${status}.`)
+    if (status === 'drained') process.exit(0)
+    if (status === 'accepting' && !signaled) {
+      process.kill(pid, 'SIGUSR2')
+      signaled = true
+    }
+    await new Promise(resolve => setTimeout(resolve, 1_000))
+  }
+  throw new Error(`Process ${pid} exited before drain completed.`)
+}
+catch (error) {
+  console.error(error instanceof Error ? error.message : error)
+  process.exit(1)
+}
diff --git a/dist/node.d.ts b/dist/node.d.ts
index 4dca1f0996d3273ec745c2fbe1097383388f97ad..1b78af6d567b0540cda4a7860ae7655d724983be 100644
--- a/dist/node.d.ts
+++ b/dist/node.d.ts
@@ -10,5 +10,25 @@ interface NodeRuntimeResourceInspectorOptions {
 }
 declare function resolveLinuxCgroupV2Path(mountinfo: string, membership: string): string | undefined;
 declare function nodeRuntimeResources(options?: NodeRuntimeResourceInspectorOptions): RuntimeResourceInspector;
+type ProcessReconcilerStatus = "accepting" | "drained" | "draining" | "failed";
+interface ProcessReconcilerRunContext {
+  track<T>(work: Promise<T>): Promise<T>;
+}
+interface ProcessReconcilerOptions {
+  intervalMs: number;
+  onDrained?: () => void | Promise<void>;
+  onError?: (error: unknown, reason: string) => void | Promise<void>;
+  onQuiesce?: () => void | Promise<void>;
+  repairReason?: string;
+  run: (reason: string, context: ProcessReconcilerRunContext) => void | Promise<void>;
+  signal?: false | NodeJS.Signals;
+}
+declare function createProcessReconciler(options: ProcessReconcilerOptions): {
+  close: () => Promise<void>;
+  drain: () => Promise<void>;
+  status: () => ProcessReconcilerStatus;
+  track: ProcessReconcilerRunContext["track"];
+  wake: (reason: string) => void;
+};
 //#endregion
-export { NodeRuntimeResourceInspectorOptions, nodeRuntimeResources, resolveLinuxCgroupV2Path };
\ No newline at end of file
+export { NodeRuntimeResourceInspectorOptions, ProcessReconcilerOptions, ProcessReconcilerRunContext, ProcessReconcilerStatus, createProcessReconciler, nodeRuntimeResources, resolveLinuxCgroupV2Path };
diff --git a/dist/node.js b/dist/node.js
index 0bc3e6b88fa881aaf7885739abd32c4a0d49f128..b8e3e0375e9dc3f2c1094c137e26c738b48ad157 100644
--- a/dist/node.js
+++ b/dist/node.js
@@ -208,5 +208,101 @@ function nodeRuntimeResources(options = {}) {
 		};
 	} };
 }
+function createProcessReconciler(options) {
+	if (!Number.isFinite(options.intervalMs) || options.intervalMs <= 0) throw new TypeError("Process reconciler intervalMs must be positive.");
+	let closed = false;
+	let drainPromise;
+	let reason = "coalesced";
+	let rerun = false;
+	let running;
+	let status = "accepting";
+	let timer;
+	const tracked = new Set();
+	const track = (work) => {
+		const promise = Promise.resolve(work);
+		tracked.add(promise);
+		void promise.finally(() => tracked.delete(promise)).catch(() => {});
+		return work;
+	};
+	const scheduleRepair = () => {
+		if (closed || timer) return;
+		timer = setTimeout(() => {
+			timer = void 0;
+			wake(options.repairReason || "repair");
+		}, options.intervalMs);
+		timer.unref?.();
+	};
+	const execute = async () => {
+		if (closed) return;
+		if (running) {
+			rerun = true;
+			return running;
+		}
+		running = (async () => {
+			do {
+				rerun = false;
+				const currentReason = reason;
+				reason = "coalesced";
+				try {
+					await options.run(currentReason, { track });
+				} catch (error) {
+					await options.onError?.(error, currentReason);
+				}
+			} while (!closed && rerun);
+		})().finally(() => {
+			running = void 0;
+			if (!closed && rerun) queueMicrotask(() => void execute());
+			else scheduleRepair();
+		});
+		return running;
+	};
+	const wake = (nextReason) => {
+		if (closed) return;
+		reason = nextReason;
+		if (timer) clearTimeout(timer);
+		timer = void 0;
+		if (running) {
+			rerun = true;
+			return;
+		}
+		queueMicrotask(() => void execute());
+	};
+	const drain = () => {
+		if (drainPromise) return drainPromise;
+		drainPromise = (async () => {
+			status = "draining";
+			closed = true;
+			if (timer) clearTimeout(timer);
+			timer = void 0;
+			await options.onQuiesce?.();
+			await running;
+			await Promise.all([...tracked]);
+			status = "drained";
+			await options.onDrained?.();
+		})().catch((error) => {
+			status = "failed";
+			throw error;
+		});
+		return drainPromise;
+	};
+	const signal = options.signal === false ? void 0 : options.signal;
+	const listener = signal ? () => {
+		void drain().catch((error) => options.onError?.(error, "drain"));
+	} : void 0;
+	if (signal && listener) process.on(signal, listener);
+	return {
+		async close() {
+			try {
+				await drain();
+			} finally {
+				if (signal && listener) process.off(signal, listener);
+			}
+		},
+		drain,
+		status: () => status,
+		track,
+		wake
+	};
+}
 //#endregion
-export { nodeRuntimeResources, resolveLinuxCgroupV2Path };
+export { createProcessReconciler, nodeRuntimeResources, resolveLinuxCgroupV2Path };
diff --git a/package.json b/package.json
index 1efb0b0e509392770773c54bba049f6e39a81435..735529a382260e7f56ea79cc258a8cce1c519775 100644
--- a/package.json
+++ b/package.json
@@ -24,6 +24,9 @@
   ],
   "type": "module",
   "sideEffects": false,
+  "bin": {
+    "vitehub-drain": "./dist/drain.js"
+  },
   "types": "./dist/index.d.ts",
   "exports": {
     ".": "./dist/index.js",
