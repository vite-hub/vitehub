diff --git a/dist/index-ndjN4ydc.d.ts b/dist/index-ndjN4ydc.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..da6806f44e6521a50e0dec699ca79cf3e4bdf0ed
--- /dev/null
+++ b/dist/index-ndjN4ydc.d.ts
@@ -0,0 +1,279 @@
+//#region src/diagnostics.d.ts
+type RuntimeDiagnosticLevel = "debug" | "error" | "info" | "warn";
+type RuntimeResourceScope = "container" | "host" | "process" | "service" | "workload";
+type RuntimeResourceUnit = "bytes" | "count" | "microseconds" | "ratio";
+interface RuntimeDiagnosticError {
+  cause?: RuntimeDiagnosticError;
+  code?: number | string;
+  details?: Readonly<Record<string, unknown>>;
+  errors?: readonly RuntimeDiagnosticError[];
+  message: string;
+  name?: string;
+  requestId?: string;
+  stack?: string;
+  status?: number | string;
+  statusCode?: number | string;
+}
+interface RuntimeDiagnosticEvent {
+  attributes?: Readonly<Record<string, unknown>>;
+  component: string;
+  error?: RuntimeDiagnosticError;
+  level: RuntimeDiagnosticLevel;
+  name: string;
+  timestamp: string;
+}
+type RuntimeDiagnosticReporter = (event: RuntimeDiagnosticEvent) => MaybePromise<void>;
+interface RuntimeResourceObservation {
+  name: string;
+  scope: RuntimeResourceScope;
+  source: string;
+  unit: RuntimeResourceUnit;
+  value: number;
+}
+interface RuntimeResourceSupport {
+  reason?: "collection-failed" | "not-isolated" | "permission-denied" | "unsupported-runtime" | (string & {});
+  scope: RuntimeResourceScope;
+  source: string;
+  supported: boolean;
+}
+interface RuntimeResourceSnapshot {
+  observedAt: string;
+  observations: readonly RuntimeResourceObservation[];
+  support?: readonly RuntimeResourceSupport[];
+}
+interface RuntimeResourceInspector {
+  inspect(options?: {
+    signal?: AbortSignal;
+  }): MaybePromise<RuntimeResourceSnapshot>;
+}
+interface RuntimeDiagnosticErrorOptions {
+  includeStack?: boolean;
+  maxDepth?: number;
+  maxErrors?: number;
+  maxStringLength?: number;
+}
+declare function normalizeRuntimeDiagnosticError(error: unknown, options?: RuntimeDiagnosticErrorOptions): RuntimeDiagnosticError;
+//#endregion
+//#region src/errors.d.ts
+type ViteHubErrorDetail = boolean | null | number | string | readonly ViteHubErrorDetail[] | {
+  readonly [key: string]: ViteHubErrorDetail | undefined;
+};
+type ViteHubErrorDetails = Readonly<Record<string, ViteHubErrorDetail | undefined>>;
+interface ViteHubErrorShape<TCode extends string = string, TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> {
+  code: TCode;
+  details?: TDetails;
+  message: string;
+  name: "ViteHubError";
+  requestId?: string;
+}
+interface ViteHubErrorOptions<TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> extends ErrorOptions {
+  details?: TDetails;
+  requestId?: string;
+}
+declare class ViteHubError<TCode extends string = string, TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> extends Error {
+  readonly code: TCode;
+  readonly details?: TDetails;
+  readonly requestId?: string;
+  constructor(code: TCode, message: string, options?: ViteHubErrorOptions<TDetails>);
+  toJSON(): ViteHubErrorShape<TCode, TDetails>;
+}
+declare function getViteHubErrorShape(value: unknown): ViteHubErrorShape | undefined;
+declare function isViteHubError(value: unknown): value is ViteHubErrorShape;
+//#endregion
+//#region src/index.d.ts
+type MaybePromise<T> = T | Promise<T>;
+interface ExecutionAuthority {
+  readonly credentials: "ambient" | "none" | "provisioned" | "unknown";
+  readonly environment: "ambient" | "none" | "selected" | "unknown";
+  readonly filesystem: {
+    readonly access: "none" | "read-only" | "read-write" | "unknown";
+    readonly scope: "host" | "none" | "sandbox" | "unknown" | "workspace";
+  };
+  readonly isolation: "container" | "microvm" | "none" | "process" | "unknown";
+  readonly network: "none" | "restricted" | "unrestricted" | "unknown";
+  readonly processes: "arbitrary" | "none" | "restricted" | "unknown";
+}
+declare const noExecutionAuthority: ExecutionAuthority;
+declare const unknownExecutionAuthority: ExecutionAuthority;
+declare function isExecutionAuthority(value: unknown): value is ExecutionAuthority;
+declare function normalizeExecutionAuthority(value: unknown): ExecutionAuthority;
+type RuntimeWaitUntil = (task: Promise<unknown>) => void;
+interface RuntimeWaitUntilController {
+  flushWaitUntil(): Promise<void>;
+  waitUntil: RuntimeWaitUntil;
+}
+interface TraceContext {
+  id: string;
+  parentId?: string;
+  sampled?: boolean;
+}
+type TraceEventContentPolicy = "content" | "metadata";
+type TraceRunStatus = "completed" | "failed" | "running";
+type TraceStepStatus = "completed" | "failed" | "running";
+interface TraceEvent {
+  attributes?: Record<string, unknown>;
+  name: string;
+  timestamp?: Date | string;
+  trace?: TraceContext;
+  type: "approval" | "capability" | "error" | "lifecycle" | "policy" | "run";
+}
+interface TraceEventLogEntry extends TraceEvent {
+  sequence: number;
+  timestamp: string;
+}
+interface TraceEventLog {
+  append(event: TraceEvent): MaybePromise<TraceEventLogEntry>;
+  entries(): readonly TraceEventLogEntry[];
+}
+interface TraceEventLogOptions {
+  content?: TraceEventContentPolicy;
+  onEntry?: (entry: TraceEventLogEntry) => MaybePromise<void>;
+}
+interface TraceStepView {
+  attributes?: Record<string, unknown>;
+  durationMs?: number;
+  endTime?: string;
+  events: TraceEventLogEntry[];
+  id: string;
+  name: string;
+  startTime: string;
+  status: TraceStepStatus;
+  type: TraceEvent["type"];
+}
+interface TraceRunView {
+  durationMs?: number;
+  endTime?: string;
+  events: TraceEventLogEntry[];
+  id: string;
+  startTime: string;
+  status: TraceRunStatus;
+  steps: TraceStepView[];
+}
+interface OpenTelemetrySpanView {
+  attributes?: Record<string, unknown>;
+  endTime?: string;
+  events?: OpenTelemetrySpanEventView[];
+  name: string;
+  parentSpanId?: string;
+  spanId: string;
+  startTime: string;
+  status: {
+    code: "ERROR" | "OK";
+    message?: string;
+  };
+  traceId: string;
+}
+interface OpenTelemetrySpanEventView {
+  attributes?: Record<string, unknown>;
+  name: string;
+  time: string;
+}
+interface OpenTelemetrySpanViewOptions {
+  content?: TraceEventContentPolicy;
+}
+interface RuntimeHostContext<TRuntimeConfig = Record<string, unknown>> {
+  capabilities?: RuntimeCapabilities;
+  cloudflare?: {
+    context?: unknown;
+    durableObjectStateName?: string;
+    env?: Record<string, unknown>;
+  };
+  event?: unknown;
+  memo<T>(key: string, create: () => T): T;
+  platform?: string;
+  request?: Request;
+  runtime: string;
+  runtimeConfig?: TRuntimeConfig;
+  trace?: TraceContext;
+  traceLog?: TraceEventLog;
+  vercel?: {
+    waitUntil?: RuntimeWaitUntil;
+  };
+  flushWaitUntil?: () => Promise<void>;
+  waitUntil: RuntimeWaitUntil;
+}
+type ExecutionContext<TRuntimeConfig = Record<string, unknown>> = RuntimeHostContext<TRuntimeConfig> & {
+  runtimeConfig: TRuntimeConfig;
+};
+type ResolvedRuntimeHostContext<TRuntimeConfig = Record<string, unknown>> = ExecutionContext<TRuntimeConfig>;
+interface CapabilityHandle<TKind extends string = string, TValue = unknown> {
+  kind: TKind;
+  name?: string;
+  value: TValue;
+}
+type RuntimeCapabilityHandle<TKind extends string = string, TValue = unknown> = CapabilityHandle<TKind, TValue>;
+type RuntimeCapabilities = Record<string, CapabilityHandle | unknown>;
+interface CapabilityDefinition<TKind extends string = string, TValue = unknown> {
+  handle: CapabilityHandle<TKind, TValue>;
+  resolve(context: ExecutionContext): MaybePromise<CapabilityHandle<TKind, TValue>>;
+}
+interface Resolvable<T, TContext extends RuntimeHostContext<any> = RuntimeHostContext> {
+  resolve(context: TContext): MaybePromise<T>;
+}
+type MaybeResolvable<T, TContext extends RuntimeHostContext<any> = RuntimeHostContext> = T | Resolvable<T, TContext> | ((context: TContext) => MaybePromise<T>);
+interface PolicyContext {
+  capability?: string;
+  input?: unknown;
+  operation?: string;
+  runtime?: string;
+  subject?: unknown;
+  trace?: TraceContext;
+}
+type PolicyDecision = "allow" | "deny" | "require-approval" | "retryable-failure";
+type CapabilityPolicy = PolicyDecision | ((context: PolicyContext) => MaybePromise<PolicyDecision>);
+type ApprovalState = "proposed" | "awaiting-approval" | "approved" | "denied" | "executing" | "completed" | "failed";
+interface ApprovalRequest<TInput = unknown> {
+  id: string;
+  capability?: string;
+  input?: TInput;
+  reason?: string;
+  state: Extract<ApprovalState, "awaiting-approval" | "proposed">;
+  trace?: TraceContext;
+}
+interface ApprovalDecision {
+  approved: boolean;
+  decidedAt?: Date | string;
+  reason?: string;
+  requestId: string;
+  state: Extract<ApprovalState, "approved" | "denied">;
+}
+interface RunLifecycleHooks<TContext extends RuntimeHostContext<any> = RuntimeHostContext> {
+  approval?: (request: ApprovalRequest, context: TContext) => MaybePromise<void>;
+  error?: (error: unknown, context: TContext) => MaybePromise<void>;
+  finish?: (context: TContext) => MaybePromise<void>;
+  request?: (context: TContext) => MaybePromise<void>;
+  trace?: (event: TraceEvent, context: TContext) => MaybePromise<void>;
+}
+declare function isTraceContentAttributeKey(key: string): boolean;
+declare function emitTraceEvent<TContext extends RuntimeHostContext<any>>(context: TContext, event: TraceEvent): Promise<TraceEventLogEntry | undefined>;
+declare function createTraceEventLog(options?: TraceEventLogOptions): TraceEventLog;
+declare function deriveTraceRuns(events: Iterable<TraceEventLogEntry>): TraceRunView[];
+declare function traceEventsToOpenTelemetrySpans(events: Iterable<TraceEventLogEntry>, options?: OpenTelemetrySpanViewOptions): OpenTelemetrySpanView[];
+interface Lease {
+  expiresAt?: Date | string;
+  id: string;
+  key: string;
+  release(): MaybePromise<void>;
+}
+interface LeaseStore {
+  acquire(key: string, options?: {
+    owner?: string;
+    ttl?: number;
+  }): MaybePromise<Lease>;
+}
+declare function createExecutionContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
+declare function resolveExecutionContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
+declare function resolveRuntimeContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
+declare function defineCapability<TKind extends string, TValue>(kind: TKind, value: TValue, options?: {
+  name?: string;
+}): CapabilityHandle<TKind, TValue>;
+declare function createRuntimeWaitUntilController(options?: {
+  forward?: RuntimeWaitUntil;
+}): RuntimeWaitUntilController;
+declare function hasCapability(context: RuntimeHostContext, name: string): boolean;
+declare function getCapability<TKind extends string = string, TValue = unknown>(context: RuntimeHostContext, name: string): CapabilityHandle<TKind, TValue>;
+declare function isResolvable<T, TContext extends RuntimeHostContext<any>>(value: MaybeResolvable<T, TContext>): value is Resolvable<T, TContext>;
+declare function resolveRuntimeValue<T, TContext extends RuntimeHostContext<any>>(value: MaybeResolvable<T, TContext>, context: TContext): Promise<T>;
+declare function resolveCapabilityPolicy(policy: CapabilityPolicy | undefined, context: PolicyContext): Promise<PolicyDecision>;
+//#endregion
+export { traceEventsToOpenTelemetrySpans as $, TraceEventLogEntry as A, deriveTraceRuns as B, RuntimeHostContext as C, TraceEvent as D, TraceContext as E, TraceStepView as F, isResolvable as G, getCapability as H, createExecutionContext as I, normalizeExecutionAuthority as J, isTraceContentAttributeKey as K, createRuntimeWaitUntilController as L, TraceRunStatus as M, TraceRunView as N, TraceEventContentPolicy as O, TraceStepStatus as P, resolveRuntimeValue as Q, createTraceEventLog as R, RuntimeCapabilityHandle as S, RuntimeWaitUntilController as T, hasCapability as U, emitTraceEvent as V, isExecutionAuthority as W, resolveExecutionContext as X, resolveCapabilityPolicy as Y, resolveRuntimeContext as Z, PolicyDecision as _, RuntimeResourceSupport as _t, CapabilityHandle as a, ViteHubErrorShape as at, RunLifecycleHooks as b, ExecutionContext as c, RuntimeDiagnosticError as ct, MaybePromise as d, RuntimeDiagnosticLevel as dt, unknownExecutionAuthority as et, MaybeResolvable as f, RuntimeDiagnosticReporter as ft, PolicyContext as g, RuntimeResourceSnapshot as gt, OpenTelemetrySpanViewOptions as h, RuntimeResourceScope as ht, CapabilityDefinition as i, ViteHubErrorOptions as it, TraceEventLogOptions as j, TraceEventLog as k, Lease as l, RuntimeDiagnosticErrorOptions as lt, OpenTelemetrySpanView as m, RuntimeResourceObservation as mt, ApprovalRequest as n, ViteHubErrorDetail as nt, CapabilityPolicy as o, getViteHubErrorShape as ot, OpenTelemetrySpanEventView as p, RuntimeResourceInspector as pt, noExecutionAuthority as q, ApprovalState as r, ViteHubErrorDetails as rt, ExecutionAuthority as s, isViteHubError as st, ApprovalDecision as t, ViteHubError as tt, LeaseStore as u, RuntimeDiagnosticEvent as ut, Resolvable as v, RuntimeResourceUnit as vt, RuntimeWaitUntil as w, RuntimeCapabilities as x, ResolvedRuntimeHostContext as y, normalizeRuntimeDiagnosticError as yt, defineCapability as z };
\ No newline at end of file
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 64cc15d7ea50408e60a6dc9874400c1a9ffc9524..54c45d406be8632e9892f8926ab6ef2abaf1ac84 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,223 +1,2 @@
-//#region src/errors.d.ts
-type ViteHubErrorDetail = boolean | null | number | string | readonly ViteHubErrorDetail[] | {
-  readonly [key: string]: ViteHubErrorDetail | undefined;
-};
-type ViteHubErrorDetails = Readonly<Record<string, ViteHubErrorDetail | undefined>>;
-interface ViteHubErrorShape<TCode extends string = string, TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> {
-  code: TCode;
-  details?: TDetails;
-  message: string;
-  name: "ViteHubError";
-  requestId?: string;
-}
-interface ViteHubErrorOptions<TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> extends ErrorOptions {
-  details?: TDetails;
-  requestId?: string;
-}
-declare class ViteHubError<TCode extends string = string, TDetails extends ViteHubErrorDetails = ViteHubErrorDetails> extends Error {
-  readonly code: TCode;
-  readonly details?: TDetails;
-  readonly requestId?: string;
-  constructor(code: TCode, message: string, options?: ViteHubErrorOptions<TDetails>);
-  toJSON(): ViteHubErrorShape<TCode, TDetails>;
-}
-declare function getViteHubErrorShape(value: unknown): ViteHubErrorShape | undefined;
-declare function isViteHubError(value: unknown): value is ViteHubErrorShape;
-//#endregion
-//#region src/index.d.ts
-type MaybePromise<T> = T | Promise<T>;
-interface ExecutionAuthority {
-  readonly credentials: "ambient" | "none" | "provisioned" | "unknown";
-  readonly environment: "ambient" | "none" | "selected" | "unknown";
-  readonly filesystem: {
-    readonly access: "none" | "read-only" | "read-write" | "unknown";
-    readonly scope: "host" | "none" | "sandbox" | "unknown" | "workspace";
-  };
-  readonly isolation: "container" | "microvm" | "none" | "process" | "unknown";
-  readonly network: "none" | "restricted" | "unrestricted" | "unknown";
-  readonly processes: "arbitrary" | "none" | "restricted" | "unknown";
-}
-declare const noExecutionAuthority: ExecutionAuthority;
-declare const unknownExecutionAuthority: ExecutionAuthority;
-declare function isExecutionAuthority(value: unknown): value is ExecutionAuthority;
-declare function normalizeExecutionAuthority(value: unknown): ExecutionAuthority;
-type RuntimeWaitUntil = (task: Promise<unknown>) => void;
-interface RuntimeWaitUntilController {
-  flushWaitUntil(): Promise<void>;
-  waitUntil: RuntimeWaitUntil;
-}
-interface TraceContext {
-  id: string;
-  parentId?: string;
-  sampled?: boolean;
-}
-type TraceEventContentPolicy = "content" | "metadata";
-type TraceRunStatus = "completed" | "failed" | "running";
-type TraceStepStatus = "completed" | "failed" | "running";
-interface TraceEvent {
-  attributes?: Record<string, unknown>;
-  name: string;
-  timestamp?: Date | string;
-  trace?: TraceContext;
-  type: "approval" | "capability" | "error" | "lifecycle" | "policy" | "run";
-}
-interface TraceEventLogEntry extends TraceEvent {
-  sequence: number;
-  timestamp: string;
-}
-interface TraceEventLog {
-  append(event: TraceEvent): MaybePromise<TraceEventLogEntry>;
-  entries(): readonly TraceEventLogEntry[];
-}
-interface TraceEventLogOptions {
-  content?: TraceEventContentPolicy;
-  onEntry?: (entry: TraceEventLogEntry) => MaybePromise<void>;
-}
-interface TraceStepView {
-  attributes?: Record<string, unknown>;
-  durationMs?: number;
-  endTime?: string;
-  events: TraceEventLogEntry[];
-  id: string;
-  name: string;
-  startTime: string;
-  status: TraceStepStatus;
-  type: TraceEvent["type"];
-}
-interface TraceRunView {
-  durationMs?: number;
-  endTime?: string;
-  events: TraceEventLogEntry[];
-  id: string;
-  startTime: string;
-  status: TraceRunStatus;
-  steps: TraceStepView[];
-}
-interface OpenTelemetrySpanView {
-  attributes?: Record<string, unknown>;
-  endTime?: string;
-  events?: OpenTelemetrySpanEventView[];
-  name: string;
-  parentSpanId?: string;
-  spanId: string;
-  startTime: string;
-  status: {
-    code: "ERROR" | "OK";
-    message?: string;
-  };
-  traceId: string;
-}
-interface OpenTelemetrySpanEventView {
-  attributes?: Record<string, unknown>;
-  name: string;
-  time: string;
-}
-interface OpenTelemetrySpanViewOptions {
-  content?: TraceEventContentPolicy;
-}
-interface RuntimeHostContext<TRuntimeConfig = Record<string, unknown>> {
-  capabilities?: RuntimeCapabilities;
-  cloudflare?: {
-    context?: unknown;
-    durableObjectStateName?: string;
-    env?: Record<string, unknown>;
-  };
-  event?: unknown;
-  memo<T>(key: string, create: () => T): T;
-  platform?: string;
-  request?: Request;
-  runtime: string;
-  runtimeConfig?: TRuntimeConfig;
-  trace?: TraceContext;
-  traceLog?: TraceEventLog;
-  vercel?: {
-    waitUntil?: RuntimeWaitUntil;
-  };
-  flushWaitUntil?: () => Promise<void>;
-  waitUntil: RuntimeWaitUntil;
-}
-type ExecutionContext<TRuntimeConfig = Record<string, unknown>> = RuntimeHostContext<TRuntimeConfig> & {
-  runtimeConfig: TRuntimeConfig;
-};
-type ResolvedRuntimeHostContext<TRuntimeConfig = Record<string, unknown>> = ExecutionContext<TRuntimeConfig>;
-interface CapabilityHandle<TKind extends string = string, TValue = unknown> {
-  kind: TKind;
-  name?: string;
-  value: TValue;
-}
-type RuntimeCapabilityHandle<TKind extends string = string, TValue = unknown> = CapabilityHandle<TKind, TValue>;
-type RuntimeCapabilities = Record<string, CapabilityHandle | unknown>;
-interface CapabilityDefinition<TKind extends string = string, TValue = unknown> {
-  handle: CapabilityHandle<TKind, TValue>;
-  resolve(context: ExecutionContext): MaybePromise<CapabilityHandle<TKind, TValue>>;
-}
-interface Resolvable<T, TContext extends RuntimeHostContext<any> = RuntimeHostContext> {
-  resolve(context: TContext): MaybePromise<T>;
-}
-type MaybeResolvable<T, TContext extends RuntimeHostContext<any> = RuntimeHostContext> = T | Resolvable<T, TContext> | ((context: TContext) => MaybePromise<T>);
-interface PolicyContext {
-  capability?: string;
-  input?: unknown;
-  operation?: string;
-  runtime?: string;
-  subject?: unknown;
-  trace?: TraceContext;
-}
-type PolicyDecision = "allow" | "deny" | "require-approval" | "retryable-failure";
-type CapabilityPolicy = PolicyDecision | ((context: PolicyContext) => MaybePromise<PolicyDecision>);
-type ApprovalState = "proposed" | "awaiting-approval" | "approved" | "denied" | "executing" | "completed" | "failed";
-interface ApprovalRequest<TInput = unknown> {
-  id: string;
-  capability?: string;
-  input?: TInput;
-  reason?: string;
-  state: Extract<ApprovalState, "awaiting-approval" | "proposed">;
-  trace?: TraceContext;
-}
-interface ApprovalDecision {
-  approved: boolean;
-  decidedAt?: Date | string;
-  reason?: string;
-  requestId: string;
-  state: Extract<ApprovalState, "approved" | "denied">;
-}
-interface RunLifecycleHooks<TContext extends RuntimeHostContext<any> = RuntimeHostContext> {
-  approval?: (request: ApprovalRequest, context: TContext) => MaybePromise<void>;
-  error?: (error: unknown, context: TContext) => MaybePromise<void>;
-  finish?: (context: TContext) => MaybePromise<void>;
-  request?: (context: TContext) => MaybePromise<void>;
-  trace?: (event: TraceEvent, context: TContext) => MaybePromise<void>;
-}
-declare function isTraceContentAttributeKey(key: string): boolean;
-declare function emitTraceEvent<TContext extends RuntimeHostContext<any>>(context: TContext, event: TraceEvent): Promise<TraceEventLogEntry | undefined>;
-declare function createTraceEventLog(options?: TraceEventLogOptions): TraceEventLog;
-declare function deriveTraceRuns(events: Iterable<TraceEventLogEntry>): TraceRunView[];
-declare function traceEventsToOpenTelemetrySpans(events: Iterable<TraceEventLogEntry>, options?: OpenTelemetrySpanViewOptions): OpenTelemetrySpanView[];
-interface Lease {
-  expiresAt?: Date | string;
-  id: string;
-  key: string;
-  release(): MaybePromise<void>;
-}
-interface LeaseStore {
-  acquire(key: string, options?: {
-    owner?: string;
-    ttl?: number;
-  }): MaybePromise<Lease>;
-}
-declare function createExecutionContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
-declare function resolveExecutionContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
-declare function resolveRuntimeContext<TRuntimeConfig = Record<string, unknown>, TContext extends RuntimeHostContext<TRuntimeConfig> = RuntimeHostContext<TRuntimeConfig>>(context: TContext): TContext & ExecutionContext<TRuntimeConfig>;
-declare function defineCapability<TKind extends string, TValue>(kind: TKind, value: TValue, options?: {
-  name?: string;
-}): CapabilityHandle<TKind, TValue>;
-declare function createRuntimeWaitUntilController(options?: {
-  forward?: RuntimeWaitUntil;
-}): RuntimeWaitUntilController;
-declare function hasCapability(context: RuntimeHostContext, name: string): boolean;
-declare function getCapability<TKind extends string = string, TValue = unknown>(context: RuntimeHostContext, name: string): CapabilityHandle<TKind, TValue>;
-declare function isResolvable<T, TContext extends RuntimeHostContext<any>>(value: MaybeResolvable<T, TContext>): value is Resolvable<T, TContext>;
-declare function resolveRuntimeValue<T, TContext extends RuntimeHostContext<any>>(value: MaybeResolvable<T, TContext>, context: TContext): Promise<T>;
-declare function resolveCapabilityPolicy(policy: CapabilityPolicy | undefined, context: PolicyContext): Promise<PolicyDecision>;
-//#endregion
-export { ApprovalDecision, ApprovalRequest, ApprovalState, CapabilityDefinition, CapabilityHandle, CapabilityPolicy, ExecutionAuthority, ExecutionContext, Lease, LeaseStore, MaybePromise, MaybeResolvable, OpenTelemetrySpanEventView, OpenTelemetrySpanView, OpenTelemetrySpanViewOptions, PolicyContext, PolicyDecision, Resolvable, ResolvedRuntimeHostContext, RunLifecycleHooks, RuntimeCapabilities, RuntimeCapabilityHandle, RuntimeHostContext, RuntimeWaitUntil, RuntimeWaitUntilController, TraceContext, TraceEvent, TraceEventContentPolicy, TraceEventLog, TraceEventLogEntry, TraceEventLogOptions, TraceRunStatus, TraceRunView, TraceStepStatus, TraceStepView, ViteHubError, type ViteHubErrorDetail, type ViteHubErrorDetails, type ViteHubErrorOptions, type ViteHubErrorShape, createExecutionContext, createRuntimeWaitUntilController, createTraceEventLog, defineCapability, deriveTraceRuns, emitTraceEvent, getCapability, getViteHubErrorShape, hasCapability, isExecutionAuthority, isResolvable, isTraceContentAttributeKey, isViteHubError, noExecutionAuthority, normalizeExecutionAuthority, resolveCapabilityPolicy, resolveExecutionContext, resolveRuntimeContext, resolveRuntimeValue, traceEventsToOpenTelemetrySpans, unknownExecutionAuthority };
\ No newline at end of file
+import { $ as traceEventsToOpenTelemetrySpans, A as TraceEventLogEntry, B as deriveTraceRuns, C as RuntimeHostContext, D as TraceEvent, E as TraceContext, F as TraceStepView, G as isResolvable, H as getCapability, I as createExecutionContext, J as normalizeExecutionAuthority, K as isTraceContentAttributeKey, L as createRuntimeWaitUntilController, M as TraceRunStatus, N as TraceRunView, O as TraceEventContentPolicy, P as TraceStepStatus, Q as resolveRuntimeValue, R as createTraceEventLog, S as RuntimeCapabilityHandle, T as RuntimeWaitUntilController, U as hasCapability, V as emitTraceEvent, W as isExecutionAuthority, X as resolveExecutionContext, Y as resolveCapabilityPolicy, Z as resolveRuntimeContext, _ as PolicyDecision, _t as RuntimeResourceSupport, a as CapabilityHandle, at as ViteHubErrorShape, b as RunLifecycleHooks, c as ExecutionContext, ct as RuntimeDiagnosticError, d as MaybePromise, dt as RuntimeDiagnosticLevel, et as unknownExecutionAuthority, f as MaybeResolvable, ft as RuntimeDiagnosticReporter, g as PolicyContext, gt as RuntimeResourceSnapshot, h as OpenTelemetrySpanViewOptions, ht as RuntimeResourceScope, i as CapabilityDefinition, it as ViteHubErrorOptions, j as TraceEventLogOptions, k as TraceEventLog, l as Lease, lt as RuntimeDiagnosticErrorOptions, m as OpenTelemetrySpanView, mt as RuntimeResourceObservation, n as ApprovalRequest, nt as ViteHubErrorDetail, o as CapabilityPolicy, ot as getViteHubErrorShape, p as OpenTelemetrySpanEventView, pt as RuntimeResourceInspector, q as noExecutionAuthority, r as ApprovalState, rt as ViteHubErrorDetails, s as ExecutionAuthority, st as isViteHubError, t as ApprovalDecision, tt as ViteHubError, u as LeaseStore, ut as RuntimeDiagnosticEvent, v as Resolvable, vt as RuntimeResourceUnit, w as RuntimeWaitUntil, x as RuntimeCapabilities, y as ResolvedRuntimeHostContext, yt as normalizeRuntimeDiagnosticError, z as defineCapability } from "./index-ndjN4ydc.js";
+export { ApprovalDecision, ApprovalRequest, ApprovalState, CapabilityDefinition, CapabilityHandle, CapabilityPolicy, ExecutionAuthority, ExecutionContext, Lease, LeaseStore, MaybePromise, MaybeResolvable, OpenTelemetrySpanEventView, OpenTelemetrySpanView, OpenTelemetrySpanViewOptions, PolicyContext, PolicyDecision, Resolvable, ResolvedRuntimeHostContext, RunLifecycleHooks, RuntimeCapabilities, RuntimeCapabilityHandle, type RuntimeDiagnosticError, type RuntimeDiagnosticErrorOptions, type RuntimeDiagnosticEvent, type RuntimeDiagnosticLevel, type RuntimeDiagnosticReporter, RuntimeHostContext, type RuntimeResourceInspector, type RuntimeResourceObservation, type RuntimeResourceScope, type RuntimeResourceSnapshot, type RuntimeResourceSupport, type RuntimeResourceUnit, RuntimeWaitUntil, RuntimeWaitUntilController, TraceContext, TraceEvent, TraceEventContentPolicy, TraceEventLog, TraceEventLogEntry, TraceEventLogOptions, TraceRunStatus, TraceRunView, TraceStepStatus, TraceStepView, ViteHubError, type ViteHubErrorDetail, type ViteHubErrorDetails, type ViteHubErrorOptions, type ViteHubErrorShape, createExecutionContext, createRuntimeWaitUntilController, createTraceEventLog, defineCapability, deriveTraceRuns, emitTraceEvent, getCapability, getViteHubErrorShape, hasCapability, isExecutionAuthority, isResolvable, isTraceContentAttributeKey, isViteHubError, noExecutionAuthority, normalizeExecutionAuthority, normalizeRuntimeDiagnosticError, resolveCapabilityPolicy, resolveExecutionContext, resolveRuntimeContext, resolveRuntimeValue, traceEventsToOpenTelemetrySpans, unknownExecutionAuthority };
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index 22a79b8f1f1dc820e803f1e5b7f05715e796cfdd..5e036ddf1dfaff8c27d785b234e205678be3bac2 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -186,6 +186,90 @@ function isViteHubError(value) {
 	return getViteHubErrorShape(value) !== void 0;
 }
 //#endregion
+//#region src/diagnostics.ts
+function boundedString(value, maximum) {
+	if (typeof value !== "string") return;
+	if (value.length <= maximum) return value;
+	const suffix = `… [${value.length - maximum} chars omitted]`;
+	return maximum > suffix.length ? `${value.slice(0, maximum - suffix.length)}${suffix}` : value.slice(0, maximum);
+}
+function safeString(value) {
+	try {
+		return String(value);
+	} catch {
+		return "Unknown error";
+	}
+}
+function readProperty(value, key) {
+	try {
+		return value[key];
+	} catch {
+		return;
+	}
+}
+function scalarProperty(value, key) {
+	const property = readProperty(value, key);
+	return typeof property === "string" || typeof property === "number" && Number.isFinite(property) ? property : void 0;
+}
+function errorChildren(value, maximum) {
+	try {
+		if (!Array.isArray(value)) return;
+		const length = Math.min(value.length, maximum);
+		return Array.from({ length }, (_item, index) => readProperty(value, index));
+	} catch {
+		return [];
+	}
+}
+function normalizeError(value, state, depth) {
+	if (typeof value === "string") return { message: boundedString(value, state.maxStringLength) || "Error" };
+	if (!value || typeof value !== "object") return { message: boundedString(safeString(value), state.maxStringLength) || "Error" };
+	if (state.ancestors.has(value)) return { message: "[Circular error cause]" };
+	if (depth > state.maxDepth) return { message: "[Error cause depth exceeded]" };
+	state.ancestors.add(value);
+	try {
+		const publicError = getViteHubErrorShape(value);
+		const message = boundedString(readProperty(value, "message"), state.maxStringLength) || boundedString(safeString(value), state.maxStringLength) || "Error";
+		const name = boundedString(readProperty(value, "name"), state.maxStringLength) || boundedString(readProperty(readProperty(value, "constructor") || {}, "name"), state.maxStringLength);
+		const result = {
+			message,
+			...name ? { name } : {}
+		};
+		const code = publicError?.code || scalarProperty(value, "code");
+		const requestId = publicError?.requestId || boundedString(readProperty(value, "requestId"), state.maxStringLength);
+		const status = scalarProperty(value, "status");
+		const statusCode = scalarProperty(value, "statusCode");
+		const stack = state.includeStack ? boundedString(readProperty(value, "stack"), state.maxStringLength) : void 0;
+		if (code !== void 0) result.code = code;
+		if (publicError?.details) result.details = publicError.details;
+		if (requestId) result.requestId = requestId;
+		if (stack) result.stack = stack;
+		if (status !== void 0) result.status = status;
+		if (statusCode !== void 0) result.statusCode = statusCode;
+		const cause = readProperty(value, "cause");
+		if (cause !== void 0) result.cause = normalizeError(cause, state, depth + 1);
+		const errors = errorChildren(readProperty(value, "errors"), state.maxErrors);
+		if (errors) result.errors = errors.map((error) => normalizeError(error, state, depth + 1));
+		return result;
+	} finally {
+		state.ancestors.delete(value);
+	}
+}
+function normalizeRuntimeDiagnosticError(error, options = {}) {
+	const maxDepth = options.maxDepth ?? 4;
+	const maxErrors = options.maxErrors ?? 8;
+	const maxStringLength = options.maxStringLength ?? 4096;
+	if (!Number.isInteger(maxDepth) || maxDepth < 0) throw new TypeError("[vitehub:runtime] Diagnostic error maxDepth must be a non-negative integer.");
+	if (!Number.isInteger(maxErrors) || maxErrors < 1) throw new TypeError("[vitehub:runtime] Diagnostic error maxErrors must be a positive integer.");
+	if (!Number.isInteger(maxStringLength) || maxStringLength < 64) throw new TypeError("[vitehub:runtime] Diagnostic error maxStringLength must be an integer of at least 64.");
+	return normalizeError(error, {
+		ancestors: /* @__PURE__ */ new Set(),
+		includeStack: options.includeStack === true,
+		maxDepth,
+		maxErrors,
+		maxStringLength
+	}, 0);
+}
+//#endregion
 //#region src/index.ts
 const noExecutionAuthority = Object.freeze({
 	credentials: "none",
@@ -656,4 +740,4 @@ async function resolveCapabilityPolicy(policy, context) {
 	return typeof policy === "function" ? await policy(context) : policy;
 }
 //#endregion
-export { ViteHubError, createExecutionContext, createRuntimeWaitUntilController, createTraceEventLog, defineCapability, deriveTraceRuns, emitTraceEvent, getCapability, getViteHubErrorShape, hasCapability, isExecutionAuthority, isResolvable, isTraceContentAttributeKey, isViteHubError, noExecutionAuthority, normalizeExecutionAuthority, resolveCapabilityPolicy, resolveExecutionContext, resolveRuntimeContext, resolveRuntimeValue, traceEventsToOpenTelemetrySpans, unknownExecutionAuthority };
+export { ViteHubError, createExecutionContext, createRuntimeWaitUntilController, createTraceEventLog, defineCapability, deriveTraceRuns, emitTraceEvent, getCapability, getViteHubErrorShape, hasCapability, isExecutionAuthority, isResolvable, isTraceContentAttributeKey, isViteHubError, noExecutionAuthority, normalizeExecutionAuthority, normalizeRuntimeDiagnosticError, resolveCapabilityPolicy, resolveExecutionContext, resolveRuntimeContext, resolveRuntimeValue, traceEventsToOpenTelemetrySpans, unknownExecutionAuthority };
diff --git a/dist/node.d.ts b/dist/node.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..a668298028dcaf4682b1b7e68f810e6bc90de262
--- /dev/null
+++ b/dist/node.d.ts
@@ -0,0 +1,11 @@
+import { pt as RuntimeResourceInspector } from "./index-ndjN4ydc.js";
+
+//#region src/node.d.ts
+type ReadText = (path: string) => Promise<string>;
+interface NodeRuntimeResourceInspectorOptions {
+  now?: () => Date;
+  readText?: ReadText;
+}
+declare function nodeRuntimeResources(options?: NodeRuntimeResourceInspectorOptions): RuntimeResourceInspector;
+//#endregion
+export { NodeRuntimeResourceInspectorOptions, nodeRuntimeResources };
\ No newline at end of file
diff --git a/dist/node.js b/dist/node.js
new file mode 100644
index 0000000000000000000000000000000000000000..0cf4587d929dea5474936ad1b18e07459edc1875
--- /dev/null
+++ b/dist/node.js
@@ -0,0 +1,143 @@
+import { readFile } from "node:fs/promises";
+import { join } from "node:path";
+import { availableMemory, memoryUsage, resourceUsage } from "node:process";
+//#region src/node.ts
+function observation(name, value, scope, source, unit) {
+	return value === void 0 || !Number.isFinite(value) ? [] : [{
+		name,
+		scope,
+		source,
+		unit,
+		value
+	}];
+}
+async function optionalRead(readText, path) {
+	try {
+		return await readText(path);
+	} catch {
+		return;
+	}
+}
+function numericFile(value) {
+	if (!value || value.trim() === "max") return;
+	const result = Number(value.trim());
+	return Number.isFinite(result) ? result : void 0;
+}
+function pairs(value) {
+	return Object.fromEntries((value || "").trim().split("\n").flatMap((line) => {
+		const [key, raw] = line.trim().split(/\s+/, 2);
+		const count = Number(raw);
+		return key && Number.isFinite(count) ? [[key, count]] : [];
+	}));
+}
+function meminfo(value) {
+	return Object.fromEntries((value || "").trim().split("\n").flatMap((line) => {
+		const match = /^(\w+):\s+(\d+)\s+kB$/.exec(line);
+		return match ? [[match[1], Number(match[2]) * 1024]] : [];
+	}));
+}
+async function cgroupObservations(readText) {
+	const relative = (await optionalRead(readText, "/proc/self/cgroup"))?.split("\n").find((line) => line.startsWith("0::"))?.slice(3);
+	if (relative === void 0) return {
+		observations: [],
+		support: {
+			reason: "unsupported-runtime",
+			scope: "service",
+			source: "linux-cgroup-v2",
+			supported: false
+		}
+	};
+	const root = join("/sys/fs/cgroup", relative);
+	const [current, peak, high, max, swapCurrent, swapPeak, events, cpu] = await Promise.all([
+		optionalRead(readText, join(root, "memory.current")),
+		optionalRead(readText, join(root, "memory.peak")),
+		optionalRead(readText, join(root, "memory.high")),
+		optionalRead(readText, join(root, "memory.max")),
+		optionalRead(readText, join(root, "memory.swap.current")),
+		optionalRead(readText, join(root, "memory.swap.peak")),
+		optionalRead(readText, join(root, "memory.events")),
+		optionalRead(readText, join(root, "cpu.stat"))
+	]);
+	if (current === void 0 && events === void 0 && cpu === void 0) return {
+		observations: [],
+		support: {
+			reason: "permission-denied",
+			scope: "service",
+			source: "linux-cgroup-v2",
+			supported: false
+		}
+	};
+	const memoryEvents = pairs(events);
+	const cpuValues = pairs(cpu);
+	return {
+		observations: [
+			...observation("memory.current", numericFile(current), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.peak", numericFile(peak), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.high", numericFile(high), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.max", numericFile(max), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.swap.current", numericFile(swapCurrent), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.swap.peak", numericFile(swapPeak), "service", "linux-cgroup-v2", "bytes"),
+			...observation("memory.events.high", memoryEvents.high, "service", "linux-cgroup-v2", "count"),
+			...observation("memory.events.max", memoryEvents.max, "service", "linux-cgroup-v2", "count"),
+			...observation("memory.events.oom", memoryEvents.oom, "service", "linux-cgroup-v2", "count"),
+			...observation("memory.events.oom_kill", memoryEvents.oom_kill, "service", "linux-cgroup-v2", "count"),
+			...observation("cpu.usage", cpuValues.usage_usec, "service", "linux-cgroup-v2", "microseconds")
+		],
+		support: {
+			scope: "service",
+			source: "linux-cgroup-v2",
+			supported: true
+		}
+	};
+}
+function nodeRuntimeResources(options = {}) {
+	const now = options.now || (() => /* @__PURE__ */ new Date());
+	const readText = options.readText || (async (path) => await readFile(path, "utf8"));
+	return { async inspect() {
+		const memory = memoryUsage();
+		const usage = resourceUsage();
+		const [cgroup, linuxMeminfo] = process.platform === "linux" ? await Promise.all([cgroupObservations(readText), optionalRead(readText, "/proc/meminfo")]) : [{
+			observations: [],
+			support: {
+				reason: "unsupported-runtime",
+				scope: "service",
+				source: "linux-cgroup-v2",
+				supported: false
+			}
+		}, void 0];
+		const host = meminfo(linuxMeminfo);
+		return {
+			observedAt: now().toISOString(),
+			observations: [
+				...observation("memory.rss", memory.rss, "process", "node", "bytes"),
+				...observation("memory.heap.total", memory.heapTotal, "process", "node", "bytes"),
+				...observation("memory.heap.used", memory.heapUsed, "process", "node", "bytes"),
+				...observation("memory.external", memory.external, "process", "node", "bytes"),
+				...observation("memory.array_buffers", memory.arrayBuffers, "process", "node", "bytes"),
+				...observation("memory.max_rss", usage.maxRSS * 1024, "process", "node", "bytes"),
+				...observation("cpu.user", usage.userCPUTime, "process", "node", "microseconds"),
+				...observation("cpu.system", usage.systemCPUTime, "process", "node", "microseconds"),
+				...observation("memory.available", availableMemory(), "process", "node", "bytes"),
+				...observation("memory.available", host.MemAvailable, "host", "linux-proc", "bytes"),
+				...observation("memory.swap.free", host.SwapFree, "host", "linux-proc", "bytes"),
+				...cgroup.observations
+			],
+			support: [
+				{
+					scope: "process",
+					source: "node",
+					supported: true
+				},
+				{
+					...linuxMeminfo === void 0 ? { reason: "collection-failed" } : {},
+					scope: "host",
+					source: "linux-proc",
+					supported: linuxMeminfo !== void 0
+				},
+				cgroup.support
+			]
+		};
+	} };
+}
+//#endregion
+export { nodeRuntimeResources };
