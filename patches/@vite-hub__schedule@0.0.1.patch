diff --git a/dist/execute-CUgz27d6.js b/dist/execute-CUgz27d6.js
index 4a93b944af3f133aef7d00bddd9ce704345358ed..6736eb97744bb8e7755a8dc989feafa4f174ab89 100644
--- a/dist/execute-CUgz27d6.js
+++ b/dist/execute-CUgz27d6.js
@@ -94,7 +94,7 @@ async function startAttempt(run, store = getScheduleRunStore()) {
 	});
 	return attempt;
 }
-function toHandlerContext(run, attempt, input) {
+function toHandlerContext(run, attempt, input, waitUntil) {
 	return {
 		attemptId: attempt.id,
 		id: run.id,
@@ -102,7 +102,28 @@ function toHandlerContext(run, attempt, input) {
 		runId: run.id,
 		scheduleId: run.scheduleId,
 		scheduledAt: run.scheduledAt,
-		target: run.target
+		target: run.target,
+		waitUntil
+	};
+}
+function createLocalWaitUntil() {
+	const pending = /* @__PURE__ */ new Set();
+	let error;
+	let failed = false;
+	return {
+		async flush() {
+			while (pending.size > 0) await Promise.allSettled([...pending]);
+			if (failed) throw error;
+		},
+		waitUntil(value) {
+			const promise = Promise.resolve(value);
+			pending.add(promise);
+			promise.then(() => pending.delete(promise), (reason) => {
+				pending.delete(promise);
+				if (!failed) error = reason;
+				failed = true;
+			});
+		}
 	};
 }
 async function completeRun(run, attempt, store = getScheduleRunStore()) {
@@ -150,8 +171,11 @@ async function executeSchedule(options) {
 	if (!created) return run;
 	const runStore = options.runStore ?? getScheduleRunStore();
 	const attempt = await startAttempt(run, runStore);
+	const localWaitUntil = options.waitUntil ? void 0 : createLocalWaitUntil();
+	const waitUntil = options.waitUntil ?? localWaitUntil.waitUntil;
 	try {
-		await options.definition.handler(toHandlerContext(run, attempt, options.input));
+		await options.definition.handler(toHandlerContext(run, attempt, options.input, waitUntil));
+		await localWaitUntil?.flush();
 		return await completeRun(run, attempt, runStore);
 	} catch (error) {
 		await failRun(run, attempt, error, runStore);
@@ -164,7 +188,8 @@ async function executeStaticSchedule(options) {
 		scheduleId: options.name,
 		source: "static",
 		scheduledAt: options.scheduledAt,
-		target: options.name
+		target: options.name,
+		waitUntil: options.waitUntil
 	});
 }
 async function loadRequiredRuntimeSchedule(id, store = getRuntimeScheduleStore()) {
@@ -217,7 +242,8 @@ async function executeRuntimeSchedule(options) {
 		scheduleId: schedule.id,
 		source: "runtime",
 		scheduledAt,
-		target: schedule.target
+		target: schedule.target,
+		waitUntil: runtimeOptions.waitUntil
 	});
 }
 async function executeRuntimeScheduleWake(input, options) {
@@ -226,7 +252,8 @@ async function executeRuntimeScheduleWake(input, options) {
 		requireDue: true,
 		runtimeScheduleStore: options.runtimeScheduleStore,
 		scheduledAt: input.scheduledAt,
-		scheduleRunStore: options.scheduleRunStore
+		scheduleRunStore: options.scheduleRunStore,
+		waitUntil: options.waitUntil
 	});
 }
 //#endregion
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 884ff9fe7d4415c91cd4f7e87f9c381bf864a50a..cc8ff1717ba9ce79efa32dd578ba56f734395b8c 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -24,12 +24,14 @@ interface ExecuteScheduleOptions {
   source?: "direct" | "runtime" | "static";
   scheduledAt?: Date;
   target: ScheduleTargetName;
+  waitUntil?: (promise: PromiseLike<unknown>) => void;
 }
 interface ExecuteStaticScheduleOptions {
   cron: string;
   definition: ScheduleDefinition;
   name: string;
   scheduledAt?: Date;
+  waitUntil?: (promise: PromiseLike<unknown>) => void;
 }
 interface ExecuteRuntimeScheduleOptions {
   id: string;
@@ -37,8 +39,9 @@ interface ExecuteRuntimeScheduleOptions {
   runtimeScheduleStore?: RuntimeScheduleStore;
   scheduledAt?: Date;
   scheduleRunStore?: ScheduleRunStore;
+  waitUntil?: (promise: PromiseLike<unknown>) => void;
 }
-declare function createScheduleRun(options: Omit<ExecuteScheduleOptions, "definition">): Promise<ScheduleRunRecord>;
+declare function createScheduleRun(options: Omit<ExecuteScheduleOptions, "definition" | "waitUntil">): Promise<ScheduleRunRecord>;
 declare function executeSchedule(options: ExecuteScheduleOptions): Promise<ScheduleRunRecord>;
 declare function executeStaticSchedule(options: ExecuteStaticScheduleOptions): Promise<ScheduleRunRecord>;
 declare function executeRuntimeSchedule(options: ExecuteRuntimeScheduleOptions | string): Promise<ScheduleRunRecord>;
diff --git a/dist/runtime/driver.js b/dist/runtime/driver.js
index 4ba226700b6d9588fe9045307198f261c841e0df..68f0388361b965aeb75f1a6088b42a659929b738 100644
--- a/dist/runtime/driver.js
+++ b/dist/runtime/driver.js
@@ -228,6 +228,18 @@ async function installScheduleRuntime(options) {
 	const previousRuntimeScheduleStore = getRuntimeScheduleStore();
 	const previousScheduleRunStore = getScheduleRunStore();
 	const reportError = createErrorReporter(options.onError);
+	const pendingWork = /* @__PURE__ */ new Set();
+	function waitUntil(value) {
+		const promise = Promise.resolve(value);
+		pendingWork.add(promise);
+		promise.then(() => pendingWork.delete(promise), (error) => {
+			pendingWork.delete(promise);
+			reportError(error);
+		});
+	}
+	async function flushWaitUntil() {
+		while (pendingWork.size > 0) await Promise.allSettled([...pendingWork]);
+	}
 	const serialize = createSerializer();
 	let driver;
 	let reconciledDriver;
@@ -259,13 +271,15 @@ async function installScheduleRuntime(options) {
 							cron: staticSchedule.definition.cron,
 							definition: staticSchedule.definition,
 							name: staticSchedule.name,
-							scheduledAt: input.scheduledAt
+							scheduledAt: input.scheduledAt,
+							waitUntil
 						});
 						return;
 					}
 					await executeRuntimeScheduleWake(input, {
 						runtimeScheduleStore: options.runtimeScheduleStore,
-						scheduleRunStore: options.scheduleRunStore
+						scheduleRunStore: options.scheduleRunStore,
+						waitUntil
 					});
 				})
 			});
@@ -307,6 +321,7 @@ async function installScheduleRuntime(options) {
 	return { close() {
 		return closePromise ??= serialize(async () => {}).then(async () => {
 			await installedDriver.close?.();
+			await flushWaitUntil();
 		});
 	} };
 }
diff --git a/dist/types-DHg6hSRT.d.ts b/dist/types-DHg6hSRT.d.ts
index 57558afc68cba307495dda44918da012aa038964..c4828fdb7463e088ec5acb54a66b27efd1154d76 100644
--- a/dist/types-DHg6hSRT.d.ts
+++ b/dist/types-DHg6hSRT.d.ts
@@ -7,6 +7,7 @@ interface ScheduleRunContext<TInput = unknown> {
   scheduleId?: string;
   scheduledAt: Date;
   target?: ScheduleTargetName;
+  waitUntil(promise: PromiseLike<unknown>): void;
 }
 type ScheduleHandler<TResult = unknown, TInput = unknown> = {
   bivarianceHack(context: ScheduleRunContext<TInput>): TResult | Promise<TResult>;
diff --git a/dist/vite-DRxdwPHQ.js b/dist/vite-DRxdwPHQ.js
index fc1a85d2932192b70b8b7cc83b73c6598604ac14..692aa02a24a15de74966e18f57a070eb0d647beb 100644
--- a/dist/vite-DRxdwPHQ.js
+++ b/dist/vite-DRxdwPHQ.js
@@ -1,8 +1,9 @@
 import { i as ensureGeneratedDir, n as createRuntimeRegistryContents, r as createImportPath, t as discoverScheduleDefinitions } from "./discovery-PA4giW9J.js";
 import { readFileSync, statSync } from "node:fs";
-import { basename, dirname, normalize, relative, resolve } from "node:path";
-import { mkdir, readFile, readdir, rm, writeFile } from "node:fs/promises";
+import { basename, dirname, isAbsolute, normalize, relative, resolve } from "node:path";
+import { mkdir, readFile, readdir, rm, rmdir, writeFile } from "node:fs/promises";
 import { fileURLToPath } from "node:url";
+import { isDeepStrictEqual } from "node:util";
 import { build } from "esbuild";
 //#region ../internal/src/build/user-entry.ts
 function toSafeAppName(rootDir) {
@@ -166,6 +167,26 @@ const productName = "schedule";
 const cloudflareOutputStateFileName = "cloudflare-output.json";
 const denoCronFileName = "deno-cron.mjs";
 const generatedRegistryFileName = "registry.mjs";
+async function removeEmptyDirectories(directory, rootDir) {
+	const root = resolve(rootDir);
+	let current = resolve(directory);
+	const pathFromRoot = relative(root, current);
+	if (!pathFromRoot || pathFromRoot.startsWith("..") || isAbsolute(pathFromRoot)) return;
+	while (current !== root) {
+		try {
+			await rmdir(current);
+		} catch (error) {
+			const code = error.code;
+			if (code === "ENOENT") {
+				current = dirname(current);
+				continue;
+			}
+			if (code === "ENOTEMPTY" || code === "EEXIST") return;
+			throw error;
+		}
+		current = dirname(current);
+	}
+}
 function resolveScheduleRuntimeEntry(metaUrl = import.meta.url) {
 	const file = fileURLToPath(metaUrl);
 	const normalizedFile = file.replace(/\\/g, "/");
@@ -663,19 +684,39 @@ async function writeVercelScheduleFunctions(options, crons) {
 		await writeFile(resolve(functionDir, ".vc-config.json"), `${JSON.stringify(createNodeFunctionConfig(), null, 2)}\n`, "utf8");
 	}
 	const configFile = resolve(outputRoot, "config.json");
-	await mkdir(outputRoot, { recursive: true });
-	let vercelConfig = createVercelConfigJson();
+	let vercelConfig;
 	try {
 		vercelConfig = JSON.parse(await readFile(configFile, "utf8"));
 	} catch (error) {
 		if (error.code !== "ENOENT") throw error;
+		if (!definitions.length) {
+			await removeEmptyDirectories(functionRoot, options.rootDir);
+			return;
+		}
+		vercelConfig = createVercelConfigJson();
 	}
 	const schedulePathPrefix = "/api/vitehub/schedules/vercel/";
-	const existingCrons = vercelConfig.crons?.filter((cron) => !cron.path.startsWith(schedulePathPrefix)) ?? [];
-	vercelConfig.crons = [...existingCrons, ...definitions.map((definition) => ({
+	const previousCrons = vercelConfig.crons ?? [];
+	const existingCrons = previousCrons.filter((cron) => !cron.path.startsWith(schedulePathPrefix));
+	if (!definitions.length && existingCrons.length === previousCrons.length) {
+		await removeEmptyDirectories(functionRoot, options.rootDir);
+		return;
+	}
+	const nextCrons = [...existingCrons, ...definitions.map((definition) => ({
 		path: getVercelSchedulePath(definition.name),
 		schedule: crons.get(definition.name)
 	}))];
+	if (nextCrons.length) vercelConfig.crons = nextCrons;
+	else delete vercelConfig.crons;
+	if (!definitions.length) {
+		await removeEmptyDirectories(functionRoot, options.rootDir);
+		const outputFiles = await readdir(outputRoot);
+		if (outputFiles.length === 1 && outputFiles[0] === "config.json" && isDeepStrictEqual(vercelConfig, createVercelConfigJson())) {
+			await rm(configFile, { force: true });
+			await removeEmptyDirectories(outputRoot, options.rootDir);
+			return;
+		}
+	}
 	await writeFile(configFile, `${JSON.stringify(vercelConfig, null, 2)}\n`, "utf8");
 }
 async function createNetlifyScheduleFunctionOutputs(options) {
@@ -698,7 +739,6 @@ async function createNetlifyScheduleFunctionOutputs(options) {
 }
 async function writeNetlifyScheduleFunctions(options) {
 	const functionRoot = resolve(options.outputRoot, "functions");
-	await mkdir(functionRoot, { recursive: true });
 	const existingFiles = await readdir(functionRoot).catch((error) => {
 		if (error.code === "ENOENT") return [];
 		throw error;
@@ -712,6 +752,8 @@ async function writeNetlifyScheduleFunctions(options) {
 		functionRoot,
 		registryFile: options.registryFile
 	});
+	if (!outputs.length) return;
+	await mkdir(functionRoot, { recursive: true });
 	await Promise.all(outputs.map(async (output) => writeFile(output.file, output.source, "utf8")));
 }
 async function writeCloudflareScheduleOutput(options) {
