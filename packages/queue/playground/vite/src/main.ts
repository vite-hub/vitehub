const root = document.querySelector<HTMLDivElement>("#app")

if (root) {
  root.innerHTML = `
    <section>
      <h1>ViteHub Queue Playground</h1>
      <p>Build output is generated for Cloudflare and Vercel.</p>
    </section>
  `
}
