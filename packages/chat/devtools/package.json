{
  "name": "@vitehub/chat-devtools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "deploy": "pnpm build && wrangler deploy",
    "dev": "nuxt dev --host 127.0.0.1 --port 5174",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt prepare && vue-tsc --noEmit"
  },
  "dependencies": {
    "@nuxt/ui": "^4.6.1",
    "@vitehub/chat": "workspace:*",
    "@vitejs/devtools-kit": "catalog:",
    "nuxt": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260430.0",
    "typescript": "catalog:",
    "vue-tsc": "catalog:",
    "wrangler": "^4.87.0"
  }
}
