// oxlint-disable-next-line typescript/triple-slash-reference -- Includes ambient declarations without a runtime import.
/// <reference path="./virtual-module.d.ts" preserve="true" />

// Keeps the types-only virtual subpath safe to import at runtime.
export {}
