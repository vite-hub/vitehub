name: manual live e2e

on:
  workflow_dispatch:

concurrency:
  group: manual-live-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup-cloudflare:
    name: setup
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup

      - name: Ensure Cloudflare Queues
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_R2_BUCKET_NAME: ${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}
        run: |
          QUEUES="queue--77656c636f6d65 queue--77656c636f6d652d656d61696c"
          export QUEUES

          for queue in $QUEUES; do
            if ! npx wrangler queues list | grep -q "$queue"; then
              npx wrangler queues create "$queue"
            fi
          done

          if [ -n "$CLOUDFLARE_R2_BUCKET_NAME" ] && ! npx wrangler r2 bucket list | grep -q "$CLOUDFLARE_R2_BUCKET_NAME"; then
            npx wrangler r2 bucket create "$CLOUDFLARE_R2_BUCKET_NAME"
          fi

  live-e2e:
    name: live e2e
    needs: setup-cloudflare
    runs-on: ubuntu-latest
    environment: Production
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        framework: [vite, nitro]
        provider: [cloudflare, vercel]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup

      - run: pnpm --filter @vitehub/queue build
      - run: pnpm --filter @vitehub/kv build
      - run: pnpm --filter @vitehub/blob build

      - name: Prepare Vercel project
        id: prepare_vercel
        if: matrix.provider == 'vercel'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="playground/${{ matrix.framework }}"
          PROJECT_NAME="vitehub-playground-${{ matrix.framework }}"

          npx vercel link --yes --cwd "$APP_DIR" --project "$PROJECT_NAME" --token "$VERCEL_TOKEN"

          readarray -t VERCEL_CONFIG < <(node -e 'const { readFileSync } = require("node:fs"); const { join } = require("node:path"); const config = JSON.parse(readFileSync(join(process.argv[1], ".vercel", "project.json"), "utf8")); console.log(config.orgId); console.log(config.projectId);' "$APP_DIR")
          VERCEL_ORG_ID="${VERCEL_CONFIG[0]}"
          VERCEL_PROJECT_ID="${VERCEL_CONFIG[1]}"

          if [ -z "$VERCEL_ORG_ID" ] || [ -z "$VERCEL_PROJECT_ID" ]; then
            echo "Failed to read linked Vercel project metadata." >&2
            exit 1
          fi

          PULLED_ENV_FILE="$(mktemp)"
          trap 'rm -f "$PULLED_ENV_FILE"' EXIT
          VERCEL_ORG_ID="$VERCEL_ORG_ID" VERCEL_PROJECT_ID="$VERCEL_PROJECT_ID" \
            npx vercel env pull "$PULLED_ENV_FILE" --environment=production --yes --cwd "$APP_DIR" --token "$VERCEL_TOKEN" >/dev/null

          KV_URL="$(node .github/scripts/get-env-value.mjs "$PULLED_ENV_FILE" KV_REST_API_URL)"
          KV_TOKEN="$(node .github/scripts/get-env-value.mjs "$PULLED_ENV_FILE" KV_REST_API_TOKEN)"

          if [ -z "$KV_URL" ] || [ -z "$KV_TOKEN" ]; then
            echo "Vercel production env verification failed: KV_REST_API_URL and KV_REST_API_TOKEN must both be set on the linked project." >&2
            exit 1
          fi

          case "$KV_URL" in
            https://*) ;;
            *)
              echo "Vercel production env verification failed: KV_REST_API_URL must start with https://." >&2
              exit 1
              ;;
          esac

          echo "app_dir=$APP_DIR" >> "$GITHUB_OUTPUT"
          echo "org_id=$VERCEL_ORG_ID" >> "$GITHUB_OUTPUT"
          echo "project_id=$VERCEL_PROJECT_ID" >> "$GITHUB_OUTPUT"

      - name: Build (cloudflare × ${{ matrix.framework }})
        if: matrix.provider == 'cloudflare'
        env:
          KV_NAMESPACE_ID: ${{ secrets.CLOUDFLARE_KV_NAMESPACE_ID }}
        run: pnpm --dir playground/${{ matrix.framework }} exec nitro build --preset cloudflare-module

      - name: Build (vercel × ${{ matrix.framework }})
        if: matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel.outputs.app_dir }}"
          npx vercel env run --cwd "$APP_DIR" --token "$VERCEL_TOKEN" -e production -- pnpm exec nitro build --preset vercel

      - name: Deploy Cloudflare
        id: deploy_cloudflare
        if: matrix.provider == 'cloudflare'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_WORKERS_SUBDOMAIN: ${{ secrets.CLOUDFLARE_WORKERS_SUBDOMAIN }}
        run: |
          WORKER_NAME="vitehub-playground-${{ matrix.framework }}"
          npx wrangler --cwd "playground/${{ matrix.framework }}/.output/server" deploy --config wrangler.json --name "$WORKER_NAME"
          echo "url=https://${WORKER_NAME}.${CLOUDFLARE_WORKERS_SUBDOMAIN}.workers.dev" >> "$GITHUB_OUTPUT"
          echo "worker=$WORKER_NAME" >> "$GITHUB_OUTPUT"

      - name: Deploy Vercel
        id: deploy_vercel
        if: matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel.outputs.app_dir }}"
          DEPLOYMENT_URL=$(VERCEL_ORG_ID="$VERCEL_ORG_ID" VERCEL_PROJECT_ID="$VERCEL_PROJECT_ID" npx vercel deploy --cwd "$APP_DIR" --prebuilt --prod --yes --token "$VERCEL_TOKEN" | grep -Eo 'https://[^ ]+\.vercel\.app' | tail -n 1)
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "Failed to capture Vercel deployment URL." >&2
            exit 1
          fi

          echo "url=$DEPLOYMENT_URL" >> "$GITHUB_OUTPUT"

      - name: Resolve deployment URL
        id: url
        run: |
          if [ "${{ matrix.provider }}" = "cloudflare" ]; then
            echo "url=${{ steps.deploy_cloudflare.outputs.url }}" >> "$GITHUB_OUTPUT"
          else
            echo "url=${{ steps.deploy_vercel.outputs.url }}" >> "$GITHUB_OUTPUT"
          fi

      - name: KV Live E2E (${{ matrix.provider }} × ${{ matrix.framework }})
        run: pnpm --dir packages/kv test:e2e --mode live --provider ${{ matrix.provider }} --framework ${{ matrix.framework }} --url "${{ steps.url.outputs.url }}"

      - name: Queue Live E2E (${{ matrix.provider }} × ${{ matrix.framework }})
        timeout-minutes: 5
        run: node packages/queue/test/e2e-live.mjs --framework "${{ matrix.framework }}" --provider "${{ matrix.provider }}" --url "${{ steps.url.outputs.url }}"

      - name: Prepare Vercel Blob project
        id: prepare_vercel_blob
        if: matrix.framework == 'vite' && matrix.provider == 'vercel'
        env:
          BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="playground/vite"
          PROJECT_NAME="vitehub-playground-vite-blob"

          if [ -z "$BLOB_READ_WRITE_TOKEN" ]; then
            echo "GitHub Production secret BLOB_READ_WRITE_TOKEN must be set." >&2
            exit 1
          fi

          npx vercel link --yes --cwd "$APP_DIR" --project "$PROJECT_NAME" --token "$VERCEL_TOKEN"

          readarray -t VERCEL_CONFIG < <(node -e 'const { readFileSync } = require("node:fs"); const { join } = require("node:path"); const config = JSON.parse(readFileSync(join(process.argv[1], ".vercel", "project.json"), "utf8")); console.log(config.orgId); console.log(config.projectId);' "$APP_DIR")
          VERCEL_ORG_ID="${VERCEL_CONFIG[0]}"
          VERCEL_PROJECT_ID="${VERCEL_CONFIG[1]}"

          if [ -z "$VERCEL_ORG_ID" ] || [ -z "$VERCEL_PROJECT_ID" ]; then
            echo "Failed to read linked Vercel project metadata." >&2
            exit 1
          fi

          npx vercel env rm BLOB_READ_WRITE_TOKEN production --yes --cwd "$APP_DIR" --token "$VERCEL_TOKEN" >/dev/null 2>&1 || true
          printf '%s' "$BLOB_READ_WRITE_TOKEN" | npx vercel env add BLOB_READ_WRITE_TOKEN production --cwd "$APP_DIR" --token "$VERCEL_TOKEN" >/dev/null

          BYPASS_SECRET=$(openssl rand -hex 32)
          echo "::add-mask::$BYPASS_SECRET"
          BYPASS_SECRET="$BYPASS_SECRET" node <<'EOF'
          const projectId = process.env.VERCEL_PROJECT_ID
          const teamId = process.env.VERCEL_ORG_ID
          const token = process.env.VERCEL_TOKEN
          const secret = process.env.BYPASS_SECRET
          const url = new URL(`https://api.vercel.com/v1/projects/${projectId}/protection-bypass`)
          if (teamId)
            url.searchParams.set('teamId', teamId)

          const response = await fetch(url, {
            body: JSON.stringify({ generate: { secret } }),
            headers: {
              authorization: `Bearer ${token}`,
              'content-type': 'application/json',
            },
            method: 'PATCH',
          })
          if (!response.ok) {
            throw new Error(`Failed to configure Vercel protection bypass: ${response.status} ${await response.text()}`)
          }
          EOF

          echo "app_dir=$APP_DIR" >> "$GITHUB_OUTPUT"
          echo "org_id=$VERCEL_ORG_ID" >> "$GITHUB_OUTPUT"
          echo "project_id=$VERCEL_PROJECT_ID" >> "$GITHUB_OUTPUT"
          echo "protection_bypass=$BYPASS_SECRET" >> "$GITHUB_OUTPUT"

      - name: Build Blob Vite (cloudflare)
        if: matrix.framework == 'vite' && matrix.provider == 'cloudflare'
        env:
          BLOB_BUCKET_NAME: ${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}
          VITEHUB_VITE_MODE: blob
          VITEHUB_HOSTING: cloudflare
        run: pnpm --dir playground/vite build

      - name: Build Blob Vite (vercel)
        if: matrix.framework == 'vite' && matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel_blob.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel_blob.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel_blob.outputs.app_dir }}"
          npx vercel env run --cwd "$APP_DIR" --token "$VERCEL_TOKEN" -e production -- sh -c 'VITEHUB_VITE_MODE=blob VITEHUB_HOSTING=vercel pnpm build'

      - name: Deploy Blob Cloudflare
        id: deploy_blob_cloudflare
        if: matrix.framework == 'vite' && matrix.provider == 'cloudflare'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_WORKERS_SUBDOMAIN: ${{ secrets.CLOUDFLARE_WORKERS_SUBDOMAIN }}
        run: |
          WORKER_NAME="vitehub-playground-vite-blob"
          npx wrangler --cwd "playground/vite/dist/vite" deploy --config wrangler.json --name "$WORKER_NAME"
          echo "url=https://${WORKER_NAME}.${CLOUDFLARE_WORKERS_SUBDOMAIN}.workers.dev" >> "$GITHUB_OUTPUT"

      - name: Deploy Blob Vercel
        id: deploy_blob_vercel
        if: matrix.framework == 'vite' && matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel_blob.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel_blob.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel_blob.outputs.app_dir }}"
          DEPLOYMENT_URL=$(VERCEL_ORG_ID="$VERCEL_ORG_ID" VERCEL_PROJECT_ID="$VERCEL_PROJECT_ID" npx vercel deploy --cwd "$APP_DIR" --prebuilt --prod --yes --token "$VERCEL_TOKEN" | grep -Eo 'https://[^ ]+\.vercel\.app' | tail -n 1)
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "Failed to capture Blob Vercel deployment URL." >&2
            exit 1
          fi

          echo "url=$DEPLOYMENT_URL" >> "$GITHUB_OUTPUT"

      - name: Blob Live E2E (vite × ${{ matrix.provider }})
        if: matrix.framework == 'vite'
        env:
          VERCEL_PROTECTION_BYPASS: ${{ steps.prepare_vercel_blob.outputs.protection_bypass }}
        run: |
          if [ "${{ matrix.provider }}" = "cloudflare" ]; then
            BLOB_URL="${{ steps.deploy_blob_cloudflare.outputs.url }}"
          else
            BLOB_URL="${{ steps.deploy_blob_vercel.outputs.url }}"
          fi

          pnpm --dir packages/blob test:e2e --mode live --url "$BLOB_URL"

  sandbox-live-e2e:
    name: sandbox live e2e
    needs: setup-cloudflare
    runs-on: ubuntu-latest
    environment: Production
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        provider: [cloudflare, vercel]
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup

      - run: pnpm --filter @vitehub/sandbox build

      - name: Prepare Vercel Sandbox project
        id: prepare_vercel_sandbox
        if: matrix.provider == 'vercel'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="packages/sandbox/playground/vite"
          PROJECT_NAME="vitehub-sandbox-vite"

          npx vercel link --yes --cwd "$APP_DIR" --project "$PROJECT_NAME" --token "$VERCEL_TOKEN"

          readarray -t VERCEL_CONFIG < <(node -e 'const { readFileSync } = require("node:fs"); const { join } = require("node:path"); const config = JSON.parse(readFileSync(join(process.argv[1], ".vercel", "project.json"), "utf8")); console.log(config.orgId); console.log(config.projectId);' "$APP_DIR")
          VERCEL_ORG_ID="${VERCEL_CONFIG[0]}"
          VERCEL_PROJECT_ID="${VERCEL_CONFIG[1]}"

          if [ -z "$VERCEL_ORG_ID" ] || [ -z "$VERCEL_PROJECT_ID" ]; then
            echo "Failed to read linked Vercel project metadata." >&2
            exit 1
          fi

          echo "app_dir=$APP_DIR" >> "$GITHUB_OUTPUT"
          echo "org_id=$VERCEL_ORG_ID" >> "$GITHUB_OUTPUT"
          echo "project_id=$VERCEL_PROJECT_ID" >> "$GITHUB_OUTPUT"

      - name: Build Sandbox Vite (cloudflare)
        if: matrix.provider == 'cloudflare'
        env:
          SANDBOX_PROVIDER: cloudflare
          NITRO_PRESET: cloudflare-module
        run: pnpm --dir packages/sandbox/playground/vite build

      - name: Build Sandbox Vite (vercel)
        if: matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel_sandbox.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel_sandbox.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel_sandbox.outputs.app_dir }}"
          npx vercel env run --cwd "$APP_DIR" --token "$VERCEL_TOKEN" -e production -- sh -c 'SANDBOX_PROVIDER=vercel NITRO_PRESET=vercel pnpm build'

      - name: Deploy Sandbox Cloudflare
        id: deploy_sandbox_cloudflare
        if: matrix.provider == 'cloudflare'
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_WORKERS_SUBDOMAIN: ${{ secrets.CLOUDFLARE_WORKERS_SUBDOMAIN }}
        run: |
          WORKER_NAME="vitehub-sandbox-vite"
          npx wrangler --cwd "packages/sandbox/playground/vite/.output/server" deploy --config wrangler.json --name "$WORKER_NAME"
          echo "url=https://${WORKER_NAME}.${CLOUDFLARE_WORKERS_SUBDOMAIN}.workers.dev" >> "$GITHUB_OUTPUT"

      - name: Deploy Sandbox Vercel
        id: deploy_sandbox_vercel
        if: matrix.provider == 'vercel'
        env:
          VERCEL_ORG_ID: ${{ steps.prepare_vercel_sandbox.outputs.org_id }}
          VERCEL_PROJECT_ID: ${{ steps.prepare_vercel_sandbox.outputs.project_id }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          APP_DIR="${{ steps.prepare_vercel_sandbox.outputs.app_dir }}"
          DEPLOY_OUTPUT=$(VERCEL_ORG_ID="$VERCEL_ORG_ID" VERCEL_PROJECT_ID="$VERCEL_PROJECT_ID" npx vercel deploy --cwd "$APP_DIR" --prebuilt --prod --yes --token "$VERCEL_TOKEN" 2>&1)
          echo "$DEPLOY_OUTPUT"

          DEPLOYMENT_URL=$(printf '%s\n' "$DEPLOY_OUTPUT" | awk '/Aliased:/ { print $2 }' | tail -n 1)
          if [ -z "$DEPLOYMENT_URL" ]; then
            DEPLOYMENT_URL=$(printf '%s\n' "$DEPLOY_OUTPUT" | grep -Eo 'https://[^ ]+\.vercel\.app' | tail -n 1)
          fi

          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "Failed to capture Sandbox Vercel deployment URL." >&2
            exit 1
          fi

          echo "url=$DEPLOYMENT_URL" >> "$GITHUB_OUTPUT"

      - name: Resolve Sandbox deployment URL
        id: sandbox_url
        run: |
          if [ "${{ matrix.provider }}" = "cloudflare" ]; then
            echo "url=${{ steps.deploy_sandbox_cloudflare.outputs.url }}" >> "$GITHUB_OUTPUT"
          else
            echo "url=${{ steps.deploy_sandbox_vercel.outputs.url }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Sandbox Live E2E (${{ matrix.provider }} × vite)
        run: pnpm --dir packages/sandbox test:e2e --mode live --provider ${{ matrix.provider }} --framework vite --url "${{ steps.sandbox_url.outputs.url }}"
